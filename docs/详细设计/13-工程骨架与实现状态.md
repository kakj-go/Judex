# 13. 工程骨架与实现状态

2026-09-25 按用户要求生成正式工程。本文件记录实现边界，不修改既有产品决定。同日执行架构修订（harness 服务端化、沙箱纯执行，见详细设计 README 与 06），本文件仅同步骨架状态。

## 已落地

- `cmd/judex-server` + `internal`：Go / Gin 模块化单体，配置校验、结构化日志、请求 ID、统一错误、探针、优雅退出、前端托管。
- `cmd/judex`：查询真实服务状态。原 `cmd/judex-agent`（沙箱内 Runner 入口）已按 2026-09-25 执行架构修订删除：harness 服务端化后不再需要独立执行程序（见详细设计 06）。
- `web`：React / TypeScript / Vite，HeroUI、Tailwind、Zustand 偏好、TanStack Query 数据层；迁入工作室全部交互，保留计划卡片树等路径。
- 预览与真实 API 分开。真实登录 / 注册表单调用 API，目前返回 501，不创建虚假用户，不把密码写 localStorage。
- `tests`：Go、前端规则、浏览器 E2E、Helm 矩阵、独立 K8s 冒烟入口。
- `deploy`：Docker 多阶段构建、Helm 开发 / 生产 values、内置 / 外部 PG 和 S3、固定版本 OpenSandbox。
- 根目录 Makefile、Apache-2.0 LICENSE / NOTICE、唯一 npm workspace 锁文件。

## 尚未落地

正式注册登录和会话、项目权限、数据库迁移 / 仓储、审批等业务事务、文件上传 / S3 适配、队列和租约、Agent harness、PostgreSQL 运行上下文、OpenSandbox 客户端、项目资料只读挂载、压缩记忆及恢复。复杂页面的细部样式与原生表单控件仍需继续统一。

前端模型测试不替代服务端事务测试。预览 localStorage 不是正式数据库模型。`/readyz` 标记 `http-scaffold`，不证明数据库或 Agent 健康。本机现有可用集群（desktop-control-plane）与 Docker；真实容器部署验收尚未执行，D15 挂载 PoC 已批准，将在临时 Namespace 进行。

## 接入顺序

本轮验证：Go build / vet / tests、17 条前端规则测试、11 条浏览器 E2E、开发 / 生产 Helm lint 均通过；Helm 测试覆盖四种存储组合、自定义沙箱 Secret、复用已有 Operator 和配置拒绝。已查看前端预览及真实 API 入口截图。GNU Make 本机未安装，逐项运行了其核心命令，未宣称执行过 make。

身份认证 → 项目 / 邀请 / 职位 → 计划任务 → 提案交接事务 → 资料存储 → Agent 执行。每个模块将真实 API、前端、权限与测试一起闭环。

启动见 [根 README](../../README.md)，部署见 [deploy](../../deploy/README.md)，验证见 [tests](../../tests/README.md)。原 `docs/demo` 保留供对照，正式开发以 `web` 为入口。
