# 11 工作目录与动态上下文

状态：Q070～Q075 已确认执行期 SQLite、项目共享资料边界、浏览器能力和派生产物发布方式；用户另明确 OpenSandbox 使用容器沙箱，与 Judex 服务部署在同一 K8s 集群。具体表名、路径、挂载驱动、缓存参数和恢复细节仍为设计方案，待实现验证。

本章替换此前将上下文拆成大量 Markdown / JSON 小文件的物理实现建议。逻辑分层继续有效，但不要求每轮生成、扫描和重写这些文件。

## 1. 已确认边界

- Q070：结构化上下文使用本地 SQLite；平台正式业务事实仍以 PostgreSQL 为准，文件系统主要承载真实文件与临时产物。
- Q071：用户提出共享对象存储只读挂入沙箱、修改派生到工作目录；最终共享范围由 Q073、Q074 确认，不将此前“所有附件逐个授权投影”的建议当成已定要求。
- Q072：分析需要时允许 Agent 使用沙箱浏览器检查 HTML 的视觉和交互。
- Q073：共享资料以项目为单位，计划相关文件按计划和文件名称组织，不为每个计划建立独立共享边界。
- Q074：项目内共享业务资料取消逐文件授权，项目之间继续隔离。私人提示词、密钥、私有执行上下文不进入共享区。
- Q075：完成一次明确输出或主动提交后，由平台登记新的共享草稿；工作区临时内容不实时公开，原件不覆盖，正式采纳仍走既有决定规则。

资料可读不等于可以修改正式工作、批准提案或验收。Q068 的中心协调仍有效，共享目录不意味着岗位 Agent 可以自由互相委派或共享可变上下文。

## 2. 四层存储与信息组织

| 层 | 内容 | 地位 |
| --- | --- | --- |
| 平台 PostgreSQL | 工作、决定、材料元数据、会话、运行事件与恢复记录 | 权威持久事实 |
| 项目对象存储共享区 | 已登记原件、版本和已提交的共享草稿 | 项目内统一可读，Agent 挂载只读 |
| 执行期 SQLite | 当前上下文、工作副本、会话窗口、材料索引、子调用状态与游标 | 可重建缓存，不能取代平台事实 |
| 运行层内存与单次模型请求 | 当前有效上下文块、查询缓存与消息组装 | 有限工作集，按版本更新 |

不把一个项目的全部业务表复制进每个 SQLite，也不把所有 Agent 的 SQLite 放进共享桶共同读写。

建议每个活跃执行上下文有独立缓存，由运行层作为唯一写入方；同一会话续接可在有效范围内复用。并行子 Agent 分别使用缓存与工作目录。同一身份并行分支不得竞争改写同一份缓存。

## 3. 目录与计划文件命名

以下路径为示意，具体命名在实现时固定：

```text
/project/                                本项目共享资料，只读
  common/                               项目公共资料
  plans/{planId}/{materialId}/v3/
    计划名称-页面方案.html
    assets/                             保留 HTML 包内相对路径
  submissions/{submissionId}/           项目直属 / 尚未归计划的资料

/workspace/                             本次运行独立可写
  work/                                 从原件派生的修改副本
  scratch/                              临时脚本、分析文件与缓存
  output/                               准备提交的新产物

运行层私有位置/
  context.db                            可信运行层维护的本地 SQLite
```

项目是共享单位；计划目录或文件名称只是组织方式，不增加计划级读取授权。可读文件名应标识计划、用途和版本，存储键仍关联稳定 ID，避免计划改名或同名文件造成覆盖。

HTML 等材料包内部文件名和相对路径不能被统一前缀重命名而破坏依赖。计划命名作用于包入口或外层目录。一个原件被多个计划引用时，元数据关联同一版本，不复制多份互相漂移的原件。

context.db 不因放在“隐藏目录”就获得保护，必须由执行身份与文件系统边界限制 Agent 任意写入。write/edit/bash 可以操作工作目录，不能靠改本地任务状态行取得平台认可。

## 4. 项目共享区与物理 Bucket

逻辑上每个项目只有自己的共享资料区；一计划一桶不作为当前设计。

实现方案：内置 SeaweedFS 可采用一项目一 Bucket；外部 S3 若允许创建 Bucket，可以使用相同模式。若外部服务只提供指定 Bucket，可将项目映射为独立前缀，但凭据、挂载根和查询范围必须限制到项目，不能顺带暴露其他项目内容。

物理 Bucket / 前缀映射方式仍是适配方案，Q073 确认的是项目级共享单位。共享区只保存可向项目公开的业务资料，系统不得把私人提示词、密钥、context.db、完整私有调试上下文或临时工作目录自动打包上传。

项目内不再维护逐文件成员白名单。项目退出、跨项目读取、共享区之外的私人配置访问仍然校验。系统产生的私有信息不能借摘要或派生产物自动公开。

## 5. SQLite 表与更新方案

| 表 | 用途 |
| --- | --- |
| context_blocks | 岗位、流程、个人偏好等有来源与版本的上下文块 |
| work_snapshot | 当前工作目标、范围、完成条件及正式状态副本 |
| decision_refs | 必须遵守的正式决定和审阅版本 |
| session_messages | 当前会话窗口，工具调用与返回关联 |
| delegations | 子运行、部分 / 最终结果、失败与仍等待项 |
| open_questions | 未决问题、异议、缺失依据 |
| materials / material_entries | 项目资料索引、材料包结构、版本、挂载路径和可读状态 |
| context_manifests / checkpoints | 每次模型输入依据、事件游标与恢复位置 |

表名属于工程方案。元数据采用按 ID / 版本增量更新，相关变化尽量在一个本地事务中应用；ContextBuilder 复用内存块，不每轮导出 Markdown 再读回来。

SQLite 仍有文件 I/O、写入串行和检查点成本。建议使用执行实例本地文件系统，不把 WAL 数据库放在 S3/FUSE 或跨节点共享网络文件系统。日志与模型流片段按合理粒度合并，不每 token 独立提交。

用户输入、正式决定、会产生副作用的工具执行记录及关键产物必须可靠保存到平台后确认成功。SQLite 中尚未同步的临时索引可重建；不能以“本地已写入”为由向用户宣称平台已持久保存。

重建缓存按持久游标与工作集恢复，不复制整个项目历史。授权、人员绑定或来源版本失效时丢弃受影响缓存，不能将旧缓存当作继续操作的许可。

## 6. 动态挂载、缓存与可见性

容器沙箱与 Judex 服务运行在同一 K8s 集群，但不放在同一个业务 Pod 中。建议项目共享区在创建沙箱时以只读卷 / 文件系统视图提供，运行过程中通过已有挂载读取新增版本，而不是每个附件热加一个 PVC。

内置 SeaweedFS 的原生 Filer / CSI 路径与外部通用 S3 挂载不是同一种适配。外部 S3 需要独立文件系统或缓存层验证，不能直接将任意 S3 endpoint 当成 SeaweedFS Filer。具体驱动、版本、多节点只读能力和性能待 PoC。

只读要同时落实在挂载与凭据 / 网络边界。Agent 不持有原件写凭据或存储管理权限；使用原生 Filer 路径时也不能假定所有请求都会经过 S3 网关授权。挂载动作由可信基础设施完成，不将挂载管理权限交给 Agent。

OpenSandbox 预热 Pool 的卷来自预先声明的模板，不应假定领取预热沙箱时能追加任意项目 PVC。首期建议先验证按项目按需创建沙箱，再评估受控复用，不能预挂全部项目换取启动速度。

共享挂载不意味着目录缓存即时刷新，也不意味着读取没有网络开销。平台登记材料后发出事件，运行层更新 SQLite 索引、刷新必要目录 / 缓存并核实固定版本路径可读。路径尚不可用时明确标记 preparing / failed，不能忽略附件后继续宣称分析完整。

缓存按项目、材料版本和摘要隔离。默认使用索引发现资料，避免每个 Agent 不断递归扫描整个共享桶。挂载驱动和缓存策略未验证前，不承诺具体用户数或并发提升。

## 7. “HTML + 文字”是一份输入

提交记录同时保存用户文字、附件及材料包的 ID / 版本。原件写入对象存储，平台校验后登记，不能把文字和附件拆成彼此无法追溯的记录。

SQLite 接收材料清单和状态；模型每轮输入明确列出本轮附件、用途和可读路径。任务要求检查页面时，不得只分析文字或 HTML 源码后宣称已经检查了视觉和交互。

HTML 的 CSS、图片、脚本保持相对路径；包内缺失资源、外部资源、后端接口或登录态未提供时，展示相应限制。Q072 允许按分析需要在容器沙箱使用浏览器，浏览器不继承生产登录态或额外项目权限。

文件存在、挂载可读、工具返回内容、页面已渲染和交互已测试分别记录。读取日志只能证明工具返回了内容，不能证明模型正确理解；普通 bash 日志也不能自动证明已读完某个目录。

## 8. 派生、编辑与发布

```text
项目共享区固定版本原件（只读）
  → 复制需要的文件 / 完整材料包到本次 work 目录
  → Agent 修改、分析、浏览器检查
  → 完成明确输出或主动提交
  → 平台校验并登记新材料版本，记录来源与运行身份
  → 作为共享草稿进入本项目资料区
  → 正式采纳 / 接受交付仍按既有决定规则执行
```

首版建议显式复制派生，不强制引入透明 OverlayFS。未提交的工作目录不实时公开，也不自动同步回原件路径。

发布记录至少包含新材料 ID / 版本、来源材料 ID / 版本、作者或代表身份、run_id、修改说明、文件清单与摘要。内容完整登记后才发出 available 事件；幂等提交不产生重复版本。

共享草稿不等于正式成果。删除、覆盖原件和修改任务正式状态不是文件挂载授予的权限。原件版本保持可追溯，不以同名覆盖方式吞掉上一次审阅依据。

## 9. 动态上下文构建

每次模型调用前，ContextBuilder 核验身份 / 当前绑定，消费相关持久事件并查询 SQLite 有效副本，必要时回源 PostgreSQL；以一个明确的本地事务快照组装本轮输入。

逻辑层次：平台执行契约 → 项目已发布流程与节点职责 → 岗位要求 → 当前人的个人偏好 → 工作约定与正式决定 → 异议 / 缺口 / 未返回结果 → 会话摘要与近期消息 → 本轮文字、材料和工具结果。

不同层次按系统指令、状态、用户消息、工具结果等来源保留结构，不把共享文件内容都当作高权限 system prompt。项目流程必须按既定要求在每轮提供有效规则表示，不能只有文件路径而没有必要约束。

- **必带**：目标、身份、硬约束、正式决定、未决异议与仍缺的必要结果。
- **必查**：用户指定附件、当前交付版本、正式约定明确要求核对的材料；标记实际范围和缺口。
- **按需**：本项目共享区其他背景、历史案例与代码细节，可自主搜索 / 读取，不需逐文件授权。

主 Agent 附带的材料清单是子 Agent 的起点，不是其项目共享资料访问上限。共享可读不等于每轮全量加载；选择相关性与读取范围属于分析过程，不能改写业务权限或新增硬条件。

本轮 context_manifest 记录绑定、流程、工作与决定版本、材料版本、消息覆盖、子结果集合、事件游标和模型配置。预算不足时压缩探索或分段分析，不静默截掉硬规则。模型压缩主要处理探索历史，正式事实每轮从权威状态重建。

## 10. 主子协作与连续性

主 Agent 的委派包含目标、工作版本、问题、材料引用、预算和返回要求。子 Agent 的岗位、个人偏好和同议题历史由平台另行组装，不复制主 Agent 的全部会话。

返回包区分结论、依据、异议、缺失项、部分 / 最终 / 失败状态和已登记产物。平台维护预期参与者与执行状态；未返回不是没有意见，失败不是通过。Q067 允许已返回结果先被处理，同一主会话串行消费事件。

Q075 的已提交产物成为项目共享草稿后，其他项目内 Agent 可自行查阅；这不是未经主 Agent 决定就主动发消息、拉人或扩展分工。

恢复依靠平台持久事件和检查点；SQLite 与临时目录可重建。更换绑定人保留业务历史，但前任的私人偏好和私有运行上下文不自动交接。项目成员关系或执行授权撤回时停止 / 隔离旧运行、撤销继续读取的能力；已经提供给模型的内容不能被声称技术上撤回。

## 11. 验收与性能验证

必须验证：

1. 两个本项目身份可读取同一共享文件，不需逐文件授权；其他项目身份不能读取或列出其内容。
2. 私人提示词、密钥、context.db 和临时输出不会自动出现在共享区。
3. 写 / 删除原件失败，派生编辑成功，明确提交后生成可追溯的新共享草稿。
4. HTML 与依赖文件完整，文字和附件保持提交关联；未渲染 / 未测试部分不伪记为已检查。
5. 运行中上传新版本，SQLite 索引、挂载缓存、可读状态与模型输入不混用版本。
6. 子 Agent 部分返回、失败和压缩后续接仍保留异议与未完成事项。
7. 多 Agent 并行不共写一个 SQLite，冷启动、批量更新、写入等待、目录缓存、实际下载量和并发内存均有测量。
8. 内置 SeaweedFS 和外部 S3 分别通过挂载、只读、动态可见性和回收测试；沙箱销毁不删除项目共享数据。

不能以采用 SQLite、挂载成功或模型声称已读完作为性能 / 理解正确的保证。本章采用的挂载适配和具体缓存策略仍须验证。
