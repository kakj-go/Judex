# Judex 重新设计讨论

日期：2026-09-25。状态：V2-Q001～Q055 与后续 Q056～Q064 技术 / 身份补充已收束到 [详细设计](./详细设计/README.md)。界面演进保留在第 38 节之后，当前只有工作室 2.0 交互 Demo；正式 UI 已选择 HeroUI，视觉不必照搬 Demo。不代表真实 Agent、CLI、服务端权限或生产工作流已实现。

## 1. 用户本轮明确的方向

1. 希望做一个好的项目协同平台，机器人在其中工作，人类掌握决策。
2. 成员的具体工作主要使用各自本地的 AI 工具完成。线上 AI 承担连接、协同这些工作的作用。
3. 需要适配更多工作场景，重新审视此前偏向编码领域的设计。
4. 保留机器人作为稳定主体、人绑定机器人且可以替换的构想。
5. 参考 larksuite/cli 的 CLI + Skills 接入方式；讨论本地提交内容能否充分自由。
6. 此前设计已归档到 `history/v1`，仅是历史设想，不自动继承为本轮约束。
7. V2-Q001：允许线上 AI 自主协调，但内容应处于草稿态，经人决策后才能进入正式内容。不能把上一轮“仅重要取舍需人决策”的建议当作已经接受的规则。
8. V2-Q002：允许一个人绑定多个机器人。该答复没有一并确定单个机器人的全部任职、代理和审批规则。
9. V2-Q003：由用户手动调用（@）Skill 获取平台事项，平台不主动触发本地 AI。该边界不是仅限首版的临时方案，也不默认安排后台自动获取或后续主动触发能力。
10. V2-Q004：扩展场景讨论，包含研发、设计协作，并提出更多场景。具体首批用户和实施优先级尚未确定。
11. V2-Q005：用户提出项目创建时用文字制定流程、配置不同环节的参与机器人；提出复用 Pi 风格 harness、主 Agent 通过 `list_agent` / `call_agent` 居中调度、子 Agent 在权限内探索并支持并行的候选架构，要求结合现有实现继续讨论。具体工具契约与工程方案尚未批准。
12. V2-Q006：按议题提案确认，支持整体接受、部分接受和退回补充，提示相互依赖，避免批准后矛盾。
13. V2-Q007：按职责和授权决定；涉及其他人的工作承诺，需相应有权人或已获统筹权限的负责人决定。机器人绑定关系不扩大审批权。
14. V2-Q008：材料可以先登记，保留原文、来源和时间；正式采纳结论、接受交付、修改工作安排仍由人决定。人在确认时可以补充材料用途。
15. V2-Q009：接受文字流程经澄清和人工确认后形成参与与授权约束，工作方法保持灵活。
16. V2-Q010：主 Agent 一个身份、多个议题会话，共享项目正式内容，关联议题通过引用衔接。
17. V2-Q011：主 Agent 不自动获得全部权限，只读取获准的项目资料和可共享回复，转交时检查接收范围。
18. V2-Q012：同一议题续接子 Agent 会话，跨议题按需加载业务知识，单次探索上下文分别管理；记忆开源项目正在调研，未选择具体产品。
19. V2-Q013：未确认经验允许作为带依据的线索跨议题使用，标明“AI 归纳、待验证”，不能直接成为正式规则或承诺。
20. V2-Q014：记忆默认限定当前项目及已授权共享知识；通用经验经过确认和明确授权后再跨项目复用，保留来源与适用条件。
21. V2-Q015：允许线上主 Agent 按项目流程自动触发或续接，同时保留手动入口；连续相关材料可合并处理，无新信息或等待人决定时停止本轮运行，保留等待状态，避免重复讨论。不改变用户手动获取本地事项的边界。
22. V2-Q016：允许在项目内提交材料时不指定议题，AI 提出关联或新议题草稿，有歧义时追问用途，材料保留原权限；这些提议必须由人确认后才正式生效，明确或高置信度不能成为自动确认的例外。
23. V2-Q017：保留明确的审阅版本；新信息形成修订草稿并提示变化；人的决定绑定实际审阅内容，提交决定前检查相关依据和依赖是否变化。
24. V2-Q018：Skill/CLI 可以获取事项列表并回复同意；Web 在 Agent 讨论过程中呈现仅对应有权人可见、可操作的审批框，并有“待我审批”卡片列表，可直接同意或跳转到对应对话位置。Q021 已进一步确认本人明确同意后由 CLI 代提交直接生效，无需再到 Web 重复确认。
25. V2-Q019：区分提供意见与正式批准，AI 汇总分歧并按流程找到有权人；需要共同同意的事项在条件未满足时保持待决，规则含糊先澄清，不要求所有发表意见者都审批。共同同意的满足方式需结合本轮 Q020 的超时规则。
26. V2-Q020：待决事项对应主管或项目创建者负责人可以同意，作为兜底；多人审批同一个问题时，一人通过后开始计时，其他未回应人超时默认同意。后续 Q022 确定代批范围，Q023 确定退回取消，Q024 确定不同步骤分别审批；时间配置与推荐默认值见 Q026。
27. V2-Q021：用户看到具体事项后明确同意，本地 AI 可通过 CLI 代为提交，按本人审批直接生效，无需 Web 再次确认；获取待办不包含自主审批授权，记录本人、来源、事项与版本。
28. V2-Q022：主管或负责人可明确选择一个或多个未回应审批项代批，记录实际操作者和覆盖范围，一键处理仍清楚列出范围；不能将代批伪记为被代批人亲自操作。
29. V2-Q023：同一个问题的提议需要所有指定审批人满足同意；审批进行中任意一人退回，则本次提议取消，退回必须明确说明原因。结合 Q020，未回应仍可按既定超时规则视为同意，明确退回不适用该规则。
30. V2-Q024：不同步骤和流程分别审批；多人计时规则针对多人审批同一个问题的情况，不把多个步骤塞进同一张审批单再做局部计时。取消后的新提议不继承旧同意与计时，已由后续 Q025 确认。
31. V2-Q025：取消后的原提议保持已取消；允许根据原因形成修订提议，展示修改说明并重新审批，不继承旧同意或倒计时；原因明确要求不再推进该方向时停止，不自动反复重提。
32. V2-Q026：审批时长采用项目默认、环节可调整；从首人同意开始展示明确截止时间，后续同意不重置。用户同时保留“默认时长目前只是建议”的说明，因此 24 个自然小时记录为推荐默认值，不作为不可调整或已锁定的数值。
33. V2-Q027：AI 从文字流程整理交付约定，缺失时帮助补齐；成果回传后对照约定提出验收建议，按已有审批规则接受后才正式完成。不同领域采用各自的成果与完成标准。
34. V2-Q028：执行过程绑定机器人，人在其中作决策；更换绑定人不影响机器人既有执行过程。新绑定的人直接进入 Chat 查看历史内容与之前审批，不设置独立交接、资料迁移或接续确认流程。
35. V2-Q029：采用机器人页面聚合议题与待决事项、进入具体议题 Chat 查看讨论/成果/审批卡片的界面结构；“待我审批”可直接操作或跳回对应位置，换人仍访问原有记录。用户进一步明确 Chat 下方有输入框，人可以作为问题、信息、证据或文件提供者辅助机器人，因此人的参与不限于审批。
36. V2-Q030：普通聊天保持讨论性质，Web 正式决定通过对应审批卡片操作，聊天中的赞同或退回措辞不自动写成正式决定。CLI 保留本人明确指定事项并同意后的直接代提交能力。

37. V2-Q031：计划代表阶段性交付，有自身目标、范围和完成条件；任务是其详细需要完成的内容。
38. V2-Q032：任务按可独立交付的成果划分，同一任务可经历开发、测试、验收等环节并由不同机器人参与；有独立交付物的测试环境、测试方案等可另建任务，不按每次岗位切换拆任务。
39. V2-Q033：议题负责讨论与结论，计划、任务记录正式工作安排；议题可以只形成结论，新增或修改工作仍经提案审批，议题结束本身不计入交付进度。
40. V2-Q034：常规任务归计划，允许临时任务直接归项目。上轮建议中的“最多一个所属计划”及跨计划复用方式未被本次答复明确确认。
41. V2-Q035：保留计划整体验收。AI 汇总已验收任务及证据，对照计划目标和完成条件提出验收建议，由流程指定的有权人确认；复用任务证据，不重做各项任务验收。
42. V2-Q036：同一项目内议题与计划、任务可双向多关联；各入口查看同一份讨论和决定，不复制聊天或重复审批，正式工作变更仍经对应提案确认。
43. V2-Q037：机器人由项目自由定义，不存在平台固定的测试等业务岗位。项目可配置多个机器人/职位模板，同模板可由多位成员分别承担，各自有一份结合模板提示词和个人提示词工作的 AI 身份；界面以关联人及其职位展示。项目通过与 AI 沟通生成多张 Mermaid 流程图，计划与工作按流程流转，职位模板可选择挂载流程，代码参与硬规则校验。具体身份持续性、节点职责、流程规则来源与运行关系继续讨论；原“固定一个主责”的建议尚未得到确认。
44. V2-Q038：认同稳定任职身份。新增同岗成员产生新身份；替换关联人保留原身份的工作、会话、待办和审批历史，不增加交接审批；个人提示词仍归本人。
45. V2-Q039：明确挂载到“哪条流程的哪个节点”。结合后续补充，该绑定帮助 AI 根据当前步骤选择职位与具体 AI 身份参与讨论；不能将这一答复扩大为全部节点动作权限细节已定。
46. V2-Q040：用户通过对话制定流程，AI 生成明确规则并据此生成 Mermaid 图和环节说明；人确认后图与规则同版生效。后续补充明确流程供 AI 参考抉择，不据此推定按图边自动执行正式状态迁移。
47. V2-Q041：计划和不同类型的任务选适用流程，各自有自己的流程运行记录；多人共同推进同一份工作，各人 AI 可有独立探索会话，同岗多人不复制任务与进度。
48. Q041 后续补充：项目流程类似知识库和系统提示词的参考，每次模型调用均带入；计划与任务创建时已有分配关联机器人，成员通过本人本地 AI/CLI 或手动更新任务；计划创建时设负责人，计划状态由负责人更新。
49. Q041 后续补充：计划可组织树形任务及前置关系；前置完成后 AI 结合流程和实际内容提出推进，由完成前项的人点击同意后交给下一人。双方卡片都展示发送人、接收人和所依据流程；接收人是否还需同意未明确，不能自动套用多人审批超时默认同意。
50. V2-Q042：认同区分当前职责交付与整项任务最终完成。本人上报交付，满足约定验收条件后确认最终完成；同一人兼有交付和验收职责时可一次明确操作完成，分别留痕。
51. V2-Q043：认同两类推进：同一任务进入下一环节，前置任务完成后推进另一独立任务。卡片区分推进类型，任务树表达拆分，前置关系表达依赖，同级任务可并行。
52. V2-Q044：接收人可以拒收，因为发送人声明完成并不一定代表实际交付已满足要求；拒收可说明原因，让发送人继续完善任务。未明确正常接收是否需点击、未回应如何处理，以及拒收是否直接撤销已正式验收结果。
53. V2-Q045：计划负责人就是该计划的整体验收人，负责整体验收及计划状态更新；不采用上轮将计划负责人和整体验收人常规分开配置的建议。
54. V2-Q046：发送后进入待接收，接收人明确接受后才算交接完成。未确认将“开始处理”默认等价为接收，也未确认接收的计时和超时规则。
55. V2-Q047：尚未最终验收的交付直接退回待完善，相关后续推进暂缓，无关任务继续。对已最终验收任务的重开方式，用户未在本次答复中确认。
56. V2-Q048：各发送人确认自己的交付，AI 汇总后呈现给接收人；接收人可以选择拒收某一发送人的内容。其它来源的接收状态是否保留、补交复核范围等仍待细化。
57. V2-Q049：按照新版流程推进，提示词直接修改；后续调用直接采用新版参考，不再要求每个已有计划/任务另行批准切换。该答复不等于允许抹掉实际发生的历史决定，已发出未接收卡片如何处理仍待细化。
58. V2-Q050：认同未变化且未受补交影响的已接收来源保留结果，仅核对补交部分；确有影响的其它内容由 AI 指出并交相关人复核，不整包清空接收意见。
59. V2-Q051：认同项目可配置接收提醒时限，到期提醒接收人和计划负责人，保持待接收，由负责人协调或按权限安排改派，不自动记为原接收人同意。
60. V2-Q052：认同新版没有实质影响的卡片继续处理；实质改变接收人或交付要求时，AI 更新建议、发送人确认改变后的内容，再交接收人处理，原记录保留。这是交接内容的确认，不是逐事项审批切版。
61. V2-Q053：认同当前有权验收的人可确认重新打开已最终验收任务；保留上一轮验收记录，修正与复核作为新一轮。超出原约定范围的新需求另建任务。
62. V2-Q054：认同一个任务最多归属一个计划，临时任务可直接归项目；其他计划通过成果引用或依赖共用原任务与验收记录，不复制工作。引用方计划仍由自己的负责人判断整体验收条件。
63. V2-Q055：认同明确确认为硬性要求的、可明确校验的业务条件由代码检查。普通推荐路线由 AI 结合实际判断；放宽硬要求由有权人明确调整。代码核对记录与材料等客观条件，成果质量仍由 AI 分析和人验收。

除以上方向及后文明确标记的用户答复外，其余均为建议或待讨论内容。尤其不自动继承原来的 Git 规则、任务状态、审批层级、目录结构、数据库与沙箱选择。

## 2. 候选产品定位

建议将 Judex 定位为：**以机器人为组织成员、围绕目标开展协作、由人掌握关键决策的 AI 协同工作空间。**

用户获得的价值是：自己的本地 AI 能持续接上团队正在做的事，并把成果交到需要它的人或机器人手中；团队可以减少反复传递背景、核对口径、追问依赖和整理决策材料的工作。

需要验证的产品假设：线上 AI 能否有效完成跨成员的理解、协调与综合，比仅提供共享聊天、文件和任务列表更有价值。此处不宣称市场独创性，也不把“机器人互相聊天”本身当作产品价值。

## 3. 机器人、人、本地 AI 的关系

建议把四个概念分开：

| 概念 | 承担什么 |
| --- | --- |
| 职位模板 | 用户在项目中定义的职责、提示词与参与要求，同模板可由多位成员承担；岗位名称任意，研究、设计、销售、开发只是示例 |
| 具体 AI 工作身份 | 同一职位模板有多份稳定任职身份；新增成员可增加身份，更换其中一个身份的关联人保留其工作与记录，Q038 已确认 |
| 当前绑定的人 | 在机器人当前职责与授权范围内决策，也可在议题中提问和提供资料；更换绑定不搬迁或重建机器人的工作 |
| 本地 AI 会话或工具 | 当前执行工作的入口，可更换模型、工具、电脑或会话，不等于机器人的永久身份 |

Q037 已明确同一职位模板可由多位成员承担，并有各自的 AI 工作身份。模板职责与个人偏好组合；具体工作的分派范围、谁被调用或参与审批仍需由项目流程和授权确定，不能把同模板自动解释为全部人都参加或共享全部上下文。

已确认一个人可以绑定多个机器人。建议每个机器人在任一时刻都有明确的主责人；不同机器人即使共享同一个人，也不应冒充两份独立的人类审核意见。后两项仍属建议。

线上机器人整理协作背景，本地 AI 获取获准的工作上下文、完成本地工作并返回成果，平台内的业务执行过程归机器人。Q028 已明确换人改变当前代表机器人决策的人，新成员直接查看该机器人 Chat 历史与已有审批，不增加交接流程。Q029 又明确人可以在 Chat 直接提问、补充信息、证据或文件；这不会把机器人的执行上下文、资料和审批进度转移到个人账号。

据此，审批位应关联机器人及其流程职责，由当前有权的绑定人操作；每次实际决定同时保留操作者身份和时间。更换绑定本身不重置任务、审批状态或已有截止时间，也不改写前任已作出的决定。原成员不再凭旧绑定操作，但如仍有独立的主管等权限，按该权限核验。这是机器人持续归属原则的实现含义，不是新的交接审批步骤。

个人设置和凭据与机器人业务记录仍需区分，Q028 不把任意私人账号内容定义为机器人业务资料；已有项目与资料访问边界继续有效。

## 4. 线上 AI 应承担的工作

线上 AI 的产出应能推动协作：

- 理解目标和现有材料，提出可执行的工作拆分与完成标准。
- 找到适合承担事项的机器人，协调责任、输入输出与依赖。
- 收到成果后判断它回答了哪些问题、缺少什么、谁还需要知道。
- 核对不同成员的成果，发现冲突的假设、相互等待和范围变化。
- 汇总已有结论，形成团队可复用的工作背景。
- 把无法自行消解的分歧整理为决策材料，给出选项、依据和影响。
- 把人的决定传播到受影响的事项和机器人，明确哪些工作需要重做或补充。

已确认：AI 自主协调产生的内容先处于草稿态，经人决策后才能进入正式内容。正式目标、分工、承诺和结论不能仅因 AI 达成共识就自动生效。提案确认粒度和授权原则见 V2-Q006、Q007。V2-Q005 提出的信息流是按项目文字流程由主 Agent 居中选择参与者和转交内容，具体执行边界继续讨论。

平台收到“请帮我买下这个服务”之类文字，不应仅凭内容就视为采购授权。自由表达、身份权限和行动授权是不同层面的设计。

## 5. 通用协作核心与领域能力

建议先围绕少量共同概念设计，而不是把软件研发状态改名后推广到所有行业。

| 通用概念 | 含义 | 软件工作示例 | 非软件工作示例 |
| --- | --- | --- | --- |
| 目标或工作主题 | 为什么开展这项协作 | 完成交付版本 | 准备品牌活动、完成客户方案 |
| 事项与责任 | 下一步要解决什么、谁负责、等待什么 | 修复登录故障 | 核实报价、补充访谈、完成视觉稿 |
| 工作贡献与成果 | 带来源的观察、材料、分析或交付物 | 代码引用、测试报告 | 访谈笔记、表格、图片、方案文件 |
| 决策 | 谁基于什么选择了什么，并影响哪些工作 | 接受延期、批准发布 | 选择主题、批准预算、接受交付 |
| 协作背景 | 已形成的目标、约束、术语、事实与待解问题 | 接口约定、环境说明 | 品牌要求、客户限制、研究假设 |

此前曾建议项目支持长期职责、周期性工作和临时咨询，并考虑不强制所有事项先挂到“项目→计划→任务”树中；该建议不等于用户确认取消计划和任务。Q031～Q033 已明确保留其业务分工；是否允许独立任务等层级例外继续讨论。

领域能力通过技能与协作模板补充。例如研发模板理解仓库、分支和 Bug；营销模板理解受众、品牌材料和渠道；研究模板理解资料来源、假设和证据。这些属于可插入的领域语义，不要求每个空间都配置。

Q027 已确认完成条件由 AI 从文字流程整理、缺失时帮助补齐。设计可验收具体稿件，研究可验收结论和依据，各领域采用自己的标准；成果回传后 AI 提供对照与验收建议，按已有审批规则接受后才正式完成。

## 6. 自由提交与可靠协作

建议允许本地 AI 自由提交文字、附件、链接、表格、图像及混合材料。文件格式不支持直接分析时仍可保存，并明确分析局限，不把“已上传”当作“已理解”。

大部分关联、摘要与分类交给 AI，常规上报不要求用户先填一套领域专用表单。但传输仍需最小上下文：身份与授权范围、所属工作空间、提交时间、来源、原始内容，以及可选的关联对象和提交意图。身份和权限由认证决定，不信任自由文本中的自我声明。

建议区分：

1. **原始贡献**：成员实际提交的内容及来源，保留原文和更正历史。
2. **AI 理解**：摘要、关联、冲突、疑问与建议，能够追溯依据和被纠正。
3. **业务决定**：经有权的人决策后进入正式内容的承诺或结论。

同一份材料可能服务多个事项。机器人可提出关联；有歧义时只追问关键缺口，不要求成员重新填写整套材料。AI 抽取的状态不应在未区分事实、推断和决定时直接成为团队共识。

V2-Q016 已明确：AI 对材料归属、关联议题或新建议题提出的建议都要人确认。材料登记可以先完成，正式关联仍待确认，不能因关联判断明确就直接生效。

Q029 的议题输入框提供了用户直接选择投递位置的入口。人在当前议题中发送消息/附件时，系统可按其明确选择记录到该 Chat，属于原始材料登记，不需要 AI 再次猜测投递位置。AI 另行提出跨议题关联、新建工作或将内容用作正式结论时，仍按 Q016、Q008 处理，不能把“收到附件”解释为正式采纳。

CLI 重试不应重复制造事项；上传时应能携带稳定的提交标识。大文件、权限和来源追溯需要工程约束，但不以固定报告章节限制表达。具体接口待产品模型确定后设计。

V2-Q008 已确认原始材料可先登记，确认时可以补充用途。建议把用途说明作为关联材料的单独记录，例如“仅供参考”“用于回答客户提出的问题”“作为本次交付候选”；保留说明者与时间，原始文件不被改写。示例用途不是强制枚举，也不要求上传时填写完成。用途发生变化后，相关 AI 理解与草稿是否需要更新，应可追溯。

## 7. 一条非编码协作示例

示例仅用于验证建议，不代表已批准的业务流程。

1. 人提出目标：“两周内完成新产品线上发布活动方案”，并给出已知限制。
2. 线上主 Agent 按项目文字流程组织相关机器人，形成研究、内容、视觉和渠道准备事项草稿；按职责与授权由有权人决定后形成正式安排。
3. 成员在本地手动调用 Skill，获取当前目标、已经作出的决定、应交付内容和相关依据。仅打开本地 AI 不代表自动获取。
4. 研究成员提交访谈笔记与表格，设计成员提交图片，渠道成员提交报价和排期，各自保留适合工作的表达方式。
5. 线上机器人发现设计方案假定的受众与研究结果不一致，而且渠道排期可能影响目标日期。
6. 子 Agent 将意见与问题返回主 Agent，由其按流程安排后续协同并形成议题提案，向有权人呈现选项、证据和受影响的工作。这里采用用户提出的中心调度方向，具体实现另见架构讨论。
7. 负责人选择方案；决定关联到已有材料，在平台上更新获准生效的内容。成员下一次手动调用 Skill 时获取相关更新，再让本地 AI 接续修改。
8. 根据事先约定的交付标准，由相应负责人确认成果；之后的执行反馈可开启新的协作事项。

需要闭合的链路：目标草稿→人的决定→正式责任→用户手动获取→本地工作→成果登记→理解与协调草稿→人的决定→下一次手动获取。确认时可补充材料用途，不能把材料登记等同于内容已被采纳。

更正、冲突、离线、等待外部答复和责任人更换都应可表达。状态展示必须区分“已发出”“对方已接收”和“已完成”，避免把消息投递当成实际进展。

## 8. CLI + Skills 的借鉴方向

从 [larksuite/cli 官方仓库](https://github.com/larksuite/cli)可借鉴：面向工作意图的快捷操作、可发现的命令结构与机器可读输出，以及通用技能和领域技能的分层。接入工具与领域工作指导可以分别演进。

Judex 候选接入层次：

- **API** 提供稳定的业务能力与身份、权限和行为记录。
- **CLI** 让本地 AI 取得背景、检查待处理事项、提交贡献、查看决定及回报结果。
- **Skills** 指导本地 AI 何时同步、如何使用背景和表达意图，领域技能再补充本行业方法。

必须双向连接。本地 AI 除了上传，还要能够拿到新的决定、待回答问题和最新上下文。

已确认：用户在本地手动调用 Skill 获取事项，类似主动打开邮件。平台保存待处理内容，等待用户获取；平台不主动触发本地 AI，也不因本地 AI 启动就自动获取。这是当前产品边界，取消上一轮对持续订阅或本地执行连接器的路线建议。

此前调研的官方 [事件 Skill](https://github.com/larksuite/cli/blob/main/skills/lark-event/SKILL.md)提供事件消费与本地事件总线；此项仅为参考事实，不纳入 Judex 当前本地接入方案。

官方 [身份与权限说明](https://github.com/larksuite/cli/blob/main/skills/lark-shared/references/lark-shared-identity-and-permissions.md)区分用户与应用身份。对 Judex 的启发是显式记录“代表哪个机器人、由哪个人或工具操作、采用什么授权”，而不是直接照搬其身份模型。

共享背景应按当前任务与权限提供，并附相关材料引用；不宜每次传递整个项目历史。过期背景和重要决定变化要能被本地 AI 察觉。

建议每次手动获取提供“自上次获取后的变化”，并带上资料版本和时间。平台可在接收成果时检查其依据是否已经变化，形成提示或协调草稿，不能假设成员一直持有最新背景。获取信息、承诺接手和完成工作应分别表达；多机器人绑定时，本次代表哪个机器人也需明确。这些交互仍待进一步设计。

## 9. 界面与体验建议

主要体验围绕三个入口组织：

- **需要我判断**：决策问题、推荐、其他选项、依据、影响范围，支持追问、修改与决定。
- **正在协作**：目标、负责的机器人、当前成果、等待和分歧，以及下一步。
- **我的机器人**：职责、进行中的事项、本地接入、工作记忆、Chat 历史、审批记录及当前绑定；换人不出现额外交接流程。

机器人之间的完整消息可展开查看，但默认呈现事实变化、当前结论和待解问题，避免大量自动聊天淹没有效信息。

决策材料应关联当时的具体成果版本。后续材料出现重要变化时，提示原决定是否需要复议，而不是让新文件悄悄替换原审批依据。AI 误解、分错对象或漏掉事实时，应能直接纠正，并看到哪些摘要和事项受影响。

不预先固定每次动作必须由多少个模型实例讨论。职责隔离与记录归属是产品语义，实际模型调度可以按需要进行；无止境的机器人对话既增加成本，也不等于达成协作。

一次协调应有明确的待解问题、输出要求和停止条件。若没有新增依据仍无法达成结论，平台应汇总分歧交给人处理，而不是继续生成相互附和的对话。

首次使用可从自然语言目标和已有文件开始，由 AI 提议工作组织方式。不同领域的模板帮助启动，用户可以修改，不把配置流程作为开始工作的前置负担。

Q029 已确认 Web 议题 Chat 下方提供输入框，人可直接提问、补充信息、证据和文件，辅助机器人。团队可以逐步接入本地 AI；没有使用 CLI 的协作者也能在 Web 提供信息。

## 10. 首轮验证与边界

用户要求扩大场景覆盖，研发与设计协作已被点名。本轮以第 13 节的九类场景检验同一协作核心；具体首批团队、验证顺序和实现范围仍待决定，不把上一轮“两类场景”的建议当作用户选择。

建议优先跑通：机器人身份与责任、用户手动获取背景、提交成果、跨成员综合草稿、一次人类决策及其在下一次获取时的传递。平台不主动触发本地 AI；不预设全面自动化执行平台或完整流程编排器。

验证时关注：为接续工作需要手工补多少背景；一次决定是否准确传到受影响者；AI 误解能否低成本纠正；需要人工维护多少台账；同一套机制能否承载第二领域。机器人发言数量和自动创建任务数量不应成为成功指标。

## 11. 第一轮答复记录

**V2-Q001：线上机器人的主动权。** 原问题提出在职责范围内自动协调、遇到关键取舍再交给人的建议。

用户答复：“可以，但是应该是作为草稿态，要人决策之后才能进入正式的内容”。

记录结果：接受自主形成草稿，正式化必须经过人的决定。不能保留“普通内部协调可自动正式生效”的解释。

**V2-Q002：人和机器人的绑定。** 原问题讨论一人可否负责多个机器人及其责任模型。

用户答复：“允许一个人绑定多个”。

记录结果：确认一人可绑定多个机器人，其他任职、代理和独立审批建议尚未由该答复确定。

**V2-Q003：本地 AI 如何接续工作。** 原问题区分按需接入和平台主动触发。

用户答复：“平台事项要用户手动@skill来获取才行，平台不应该主动触发本地AI，就像邮件一样用户自己点击才查看”。

记录结果：本地获取由用户显式发起，主动触发不属于当前产品方向，不保留为默认后续路线。该答复针对获取方式，未确定每次上传是否需要另一次手动调用。

**V2-Q004：先服务哪一类团队。** 原建议先选两类真实工作验证。

用户答复：“最好多搞几个场景，研发，设计协作 ，你再多提几个”。

记录结果：扩展场景讨论；明确包含研发、设计协作；其他场景由本轮提出，实施优先级尚未选定。

## 12. 草稿与正式内容的候选协作机制

本节同步 V2-Q005～Q008 答复；其中已确认的提案粒度、授权和材料登记原则与仍待讨论的工程建议分别表述。

建议平台同时呈现“当前正式内容”和“正在讨论的草稿”。AI 可以起草新的事项与变更，但已生效内容维持原决定，直到有人批准相应修改。

用户提出在项目文字流程中配置环节参与者，由主 Agent 选择调用机器人及转交内容，子 Agent 的问题和结果先回到主 Agent。不能将原建议解释为所有机器人自由互传消息。建议每次转交保留草稿标识和来源，避免被理解为正式任务、已承诺日期或已批准结论。草稿可见性和正式生效是两个独立问题。

已确认按议题提案作出决定，支持整体接受、部分接受、退回补充及依赖提示。建议提案说明：要决定什么、当前正式内容是什么、拟改成什么、影响哪些事项和机器人、依据哪些具体版本的材料。若部分内容互相依赖，应说明影响，不能形成矛盾的正式结果。

例如活动方案先形成正式安排；后续新增报价导致排期变更建议，则呈现原安排与新草稿的差异。按 Q024，不同问题和步骤分别提交审批，议题层面的部分接受通过选择不同审批事项表达；同一个问题中的同意针对同一份提议。若决定所依据的正式内容或材料已经变化，应提醒复核。

已确认授权原则：人确认其权限内的内容，涉及其他负责范围的承诺时，需要相应有权人决策，或由已有明确授权的负责人统筹。机器人绑定关系本身不扩大审批权；多人同意、主管代批、超时与退回规则已由 Q019～Q026 细化，具体身份与权限实现仍需设计。

已确认原始材料可以先登记并保留原文、来源、时间；确认时可以补充材料用途。任何技术上的上传成功、信息获取成功，都不能替代对内容或工作责任的正式接受。

## 13. 跨场景验证建议

以下九类用于检验同一协作核心，除研发和设计协作外均为新建议；不是九套已经确定的行业模块。

| 场景 | 本地工作与提交 | 平台 AI 草稿协同 | 人决定什么 | 对共同模型的检验 |
| --- | --- | --- | --- | --- |
| 研发交付 | 实现、分析、代码引用、测试与部署报告 | 需求和成果关联、依赖与问题归纳、交付方案 | 范围、分工、变更与交付接受 | 多角色依赖与跨版本依据 |
| 设计协作 | 图片、原型、源文件引用、评审意见 | 汇总反馈、比较备选、识别需求冲突 | 风格方向、采用版本与定稿 | 多方案并存、主观取舍与版本关系 |
| 内容与营销 | 受众资料、文案、素材、渠道数据 | 口径核对、内容安排与复盘草稿 | 主题、发布方案、资源安排 | 多种成果与统一口径 |
| 研究与调研 | 访谈、数据、资料链接、分析记录 | 汇总证据、保留不同假设、提出待验证问题 | 研究方向、可采纳结论与下一步 | 不确定性、证据来源和分歧 |
| 客户方案与交付 | 需求记录、方案、估算、反馈 | 对齐范围、整理客户变更与交付差距 | 对外承诺、交付范围、变更和验收 | 内外部参与和承诺边界 |
| 活动筹备 | 场地与供应商信息、物料、日程 | 整理准备事项、交叉依赖与备选安排 | 预算安排、方案、关键时间变更 | 实物工作、外部等待和固定日期 |
| 采购与行政协作 | 需求、报价、比较表、到货记录 | 比较选项、发现资料缺项、整理待决方案 | 采购选择、资源使用和接收结论 | 跨部门协作及本地或线下执行结果 |
| 培训与课程制作 | 课程大纲、课件、视频、试讲反馈 | 内容一致性检查、版本比较与改进草稿 | 教学目标、课程方案、定稿 | 内容组合、反馈迭代与复用 |
| 日常运营与客户支持复盘 | 问题记录、处理报告、指标、用户反馈 | 问题聚类、影响分析、改进事项草稿 | 处理策略、改进安排和结论 | 长期职责、周期工作与人员替换下的业务连续性 |

研发、设计、研究、日常运营分别检验依赖、方案选择、证据分歧和持续职责；其他案例补充对外协作、实物过程及多种成果。所有案例都保持同一边界：工作在成员使用的本地工具或实际工作环境完成，平台协调草稿并承接人的正式决定，用户主动获取后续事项。

建议将场景能力作为知识、Skills、成果呈现和协作模板，而不是让通用核心预置每个领域的完整流程。首版实际实现范围需要后续选择。

## 14. 第二轮答复记录

**V2-Q005：草稿能否在有权限的相关机器人之间流转？** 人尚未确认时，其他参与机器人能否读取并补充意见？

用户答复要点：项目创建时有文字流程，决定哪些机器人参与哪些环节；参考 Pi 的四工具、上下文压缩与管理构建统一 harness；增加 `list_agent` 和 `call_agent`，列出项目机器人及结合个人提示词的提示词，主 Agent 调度子 Agent，知道其返回内容并选择传给谁；子 Agent 自行探索权限内内容、把问题和结果返回主 Agent；支持子 Agent 并行；要求研究现有多 Agent 的协调实现。

记录结果：以此为本轮候选架构方向，比较现有实现并讨论边界，不视为所有底层技术选型已确认。详细分析和下一轮问题见 [Agent 协同架构讨论](./Agent协同架构讨论.md)。

**V2-Q006：人以什么粒度把草稿转正式？** 按每条消息逐一确认，还是围绕一个议题整理出提案，由人审阅具体变化？

用户答复：“按议题提案确认。支持整体接受、部分接受和退回补充；相互依赖的部分需要一起提示，避免批准后互相矛盾。” 已确认。

**V2-Q007：谁能把哪些内容转正式？** 绑定某机器人是否代表可以批准所有相关事项，还是由事先授予的职责与权限决定？

用户答复：“按职责和授权决定。涉及别人的工作承诺，需要相应有权的人确认，或者由已经获得统筹权限的负责人决定。机器人绑定关系本身不自动扩大审批权。” 已确认原则，具体授权模型继续讨论。

**V2-Q008：原始材料登记是否需要单独审批？** 本地上传的文件、文字和来源能否先自动记录为“已提交材料”，其结论与业务影响仍按草稿机制处理？

用户答复：“可以先登记。保留原文、来源和时间；正式采纳结论、接受交付、修改工作安排，仍需要人的决定。” 补充：“然后确认的时候可以补充这些资料是干嘛的”。已确认。

## 15. 协同闭环与第五轮确认

本节已同步 V2-Q015～Q017。明确标记的原则已获确认，补充交互和实现细节仍为建议。记忆后端、具体运行框架和 Demo 均未因此选定或实现。

已确认的核心机制可以连接为：材料登记→平台按流程协调草稿（含必要的关联或新议题建议）→形成议题提案→有权人决定→批准范围内的正式内容生效→成员手动获取→本地工作与新的材料回流。关联建议本身需要人的确认，不能由 AI 自行跨过这一环。

### 平台内协同的启动与续接

V2-Q015 已确认按项目流程自动触发，同时保留手动入口；连续提交的相关材料可以合并处理。无新信息或正在等待人决定时，保存现场并停止本轮运行，等待后续事件，不反复生成相同结论。具体事件配置、合并时机和去重机制仍待实现设计。

这里只讨论线上主 Agent 的运行时机。用户手动调用 Skill 才获取事项的本地边界已经确定，不重新讨论或改变。自动启动平台分析也不等于正式内容自动改变。

### 自由提交后的材料归属

V2-Q016 已确认提交者可以指定项目与议题，也可以在已选项目中只提供材料。未指定议题时，AI 先提出与已有议题的关联或新议题草稿；有多种合理解释时追问关键用途。用户特别补充，这些提议都要人确认，不因 AI 判断明确而直接形成正式归属。

材料原有可见范围保持有效。提出或确认关联都不能隐式扩大可见范围；无法确定关联时，建议先保留为项目内待整理材料，不阻塞登记。

### 提案、部分批准与新材料

V2-Q017 已确认人作决定时面对明确版本的提案，AI 收到新信息可以生成修订草稿并提示变化，不能静默覆盖审阅内容；人的决定绑定实际审阅的内容，提交决定前检查依据和依赖是否变化。具体版本标识、变更展示和冲突处理机制仍待设计。

按 Q024，“采用视觉方案 A”和“追加某项资源”若是不同问题或流程步骤，应形成各自独立的审批事项。议题可以集中展示它们，但不在同一问题的审批单内拆出局部计时。若方案 A 必须依赖新增资源，仍需明确展示并核对依赖，避免独立审批后形成矛盾安排。

新材料影响待决内容时，提示相关变化并生成修订草稿；已正式生效的结论不会仅凭 AI 推断自动撤销。如需改变，另行形成由有权人处理的变更提案。

## 16. 第五轮答复记录

**V2-Q015：线上主 Agent 何时启动一轮协同？** 收到与议题相关的材料、答复、人工决定或退回时，可否按项目流程自动启动或续接？

用户答复：“允许按流程自动触发，同时保留手动入口。连续提交的相关材料可以合并处理；没有新信息、正在等人决定时停止等待，避免反复讨论同一问题。” 记录为停止本轮运行并保留等待状态，不解释为自动跳过人的决定。已确认平台内触发原则。

**V2-Q016：提交材料时能否不指定议题？** 用户已选项目，但只上传内容，能否由 AI 提出关联已有议题或创建新议题草稿？

用户答复：“允许。AI 先整理关联建议，有歧义时只追问关键用途。材料保留原有权限；关联建议和由此产生的工作安排继续遵守草稿确认规则。” 补充：“但是这些提议要人来确认”。已确认，不能将“明确时少打扰”推导为自动确认。

**V2-Q017：待审提案是否固定版本？** AI 收到新材料后，能否直接替换人正在审阅的提案，还是生成新版本并明确提示变化？

用户答复：“保留明确的审阅版本，另生成修订草稿并提示变化。人的决定绑定实际审阅的内容；提交决定前检查相关依据和依赖是否已变化。” 已确认。具体条件批准或复杂依赖处理并未由此全部定型。

## 17. 人的决策入口与第六轮确认

本节同步 V2-Q018～Q020。用户明确改变了上一轮仅 Web 审批、无超时同意的建议；最新答复优先。原 V1 的固定层级仍不自动继承，本轮主管兜底与超时同意来自新的明确答复。

### 确认入口

Q018、Q021 已确认 Skill/CLI 获取列表后，用户明确同意具体事项，本地 AI 可代提交直接完成本人审批，无需 Web 重复操作。入口开放不代表本地 AI 可以自行审批一切事项；具体身份和授权实现仍需设计。

Web 已确认两种入口：Agent 讨论位置中的审批框，以及“待我审批”卡片列表；卡片可直接同意，也可跳转到对应对话位置。审批框仅向对应有权人显示并允许其操作。其他成员能看到哪些审批进度或结论，继续受相关内容权限约束，未由本答复统一公开。

为了减少重复操作，同一议题下的关联建议、用途说明和业务提案可以集中呈现，每一项清楚标明变化、依赖及谁有权确认。集中呈现不等于一并批准；需要的确认仍逐项明确记录。是否一次提交多个决定，应根据各项权限和流程允许范围设计，不能跨过材料关联的确认要求。

### 多人参与与意见冲突

Q019 已确认在项目文字流程中区分提供意见的参与者与正式批准者。AI 整理意见和分歧，按流程找到有权人；无需每个发表意见的人都参与最终审批。

对同一个问题，流程明确指定审批人；Q023 要求所有指定审批人满足同意。Q020 允许未回应人按超时规则视为同意，Q022 允许主管明确代批，记录应保留不同来源。任意一位审批人在进行中退回并说明原因，本次提议即取消，不能再到期通过。主 Agent 自行达成共识仍不等于满足审批条件。

### 无人处理与人员暂缺

Q020、Q022 已明确待决事项对应主管或项目创建者负责人可以选择一个或多个未回应审批项代批，操作记录体现实际操作者与覆盖范围。Q023 的取消结论不通过继续代批剩余项来绕过。

多人审批同一个问题时，一人通过后开始计时，其他未回应人超时默认同意；不同步骤独立审批。Q025 已确认取消后修订重提不继承旧同意与计时；Q026 已记录项目默认、环节可调及后续同意不重置，24 个自然小时仍为推荐默认值。该机制在平台运行，不触发成员本地 AI。

## 18. 第六轮答复记录

**V2-Q018：正式确认使用什么入口？** 首版是否由人登录 Web 作正式决定，Skill/CLI 负责获取、上报与提交草稿，其凭据不具备正式审批权限？

用户答复：Skill/CLI 也可以获取事项列表并回复同意；Agent 讨论中出现审批框，对应的人才能看到并点击同意；另有需要本人审批的卡片列表，可直接同意或跳转到对应对话位置。记录结果：允许 CLI/Skill 回复及两类 Web 审批入口，原先仅 Web 审批的建议不再适用；后续 Q021 已确认本人明确同意后由 CLI 代提交直接生效。

**V2-Q019：多人参与时，是否区分提供意见与正式批准？** 是否在文字流程中明确谁最终决定、哪些事项需要指定多人共同同意？

用户答复：“区分。AI 汇总意见和分歧，按流程找到有权决策的人。要求共同同意的事项，在条件未满足时保持待决；规则含糊时先澄清。这样也不必要求每个发表意见的人都参与最终审批。” 已确认。

**V2-Q020：待决事项长期无人处理时怎么办？** 是否按项目预先配置的规则提醒、转交给已授权代理人，仍无人可处理时保留待决并提示负责人补足安排？

用户答复：“待决事项对应的主管或者项目创建者负责人可以同意，所以不担心没有人理会，对于多人审批的场景其中一人审批通过之后开始计时，其他人超时默认作为同意选项。” 已确认兜底同意与超时默认同意原则；旧建议被此次答复替代。

## 19. 多入口审批与第七轮确认

本节已同步 Q021～Q026：CLI 本人指示后直接审批、明确范围的主管代批、任意一人退回则取消且必填原因、不同步骤分别审批、取消后重提及计时配置。具体运行实现仍标记为建议，24 小时作为推荐默认值。

### 统一审批内容与入口

建议讨论中的审批框、“待我审批”卡片和 CLI 列表引用同一审批事项与提案版本。卡片与 CLI 输出展示具体待决内容、变化、影响、当前审批状态及相关截止时间；快捷同意不强制要求再跳回讨论页重复操作。

讨论位置跳转应定位对应版本的审批内容，内容有权限才允许读取；审批按钮只对有权人可见，实际提交仍由服务端校验身份、权限与版本。重复点击或 CLI 重试不应重复批准、重复生效或重新启动计时。

### CLI 的本人指示

Q021 已确认：本地 AI 展示具体事项后，用户明确同意，本地 AI 通过 CLI 代为提交，按本人身份记录并标明来源、事项与版本，不再要求另到 Web 重复同意。概括性的“看看我的待办”不包含审批指示。明确多个独立事项时，可以分别记录对各项的同意，不能无说明扩展范围。

普通 API 凭据只证明调用身份和权限，不能单独证明本轮有人亲自看过内容。具体采用本地显式交互、一次性授权或其他方式，需要后续定义；不把 AI 自行填写的“human_confirmed”字段宣称为可靠的真人确认凭证。无需在本轮选定实现方式。

### 同意的来源与主管兜底

建议记录本人通过 Web/CLI 主动同意、主管或负责人代批、系统按规则超时视为同意等不同来源。记录实际操作者、被处理的审批范围、提案版本和采用的规则，不能将超时或代批写成原审批人亲自点了同意。

Q022 已确认主管兜底可明确选择一个或多个尚未回应的审批项代批，并记录真实操作者和范围。Q023 已确认一人退回即取消，因此代批不覆盖已退回，也不能使已取消提议复活。人员具有多个机器人身份时，建议同时保留实际人物与所代表职责，不制造多个独立真人操作记录。

### 超时的对象与版本

Q023 已确认审批进行中任意一人退回，本次提议取消，且必须明确说明原因。这比上一轮“继续保留分歧待处理”的建议更明确。取消后结束该审批的计时，关闭剩余待审批操作；超时不能继续将其推成通过。

Q024 已明确计时针对同一个问题的多人审批。不同步骤、不同问题各自形成审批事项，不共享首个同意或倒计时；取消后重提与计时配置以 Q025、Q026 为准。

Q026 已记录同一审批轮次从首人同意开始显示明确截止时间，后续同意不重置倒计时；项目设默认时长，环节可以调整。24 个自然小时保留为推荐默认值。重复请求不能改变已发生的首个同意时间，具体去重和截止检查属于实现细节。

Q025 已确认取消后的原提议保持已取消，修订提议展示修改说明并重新审批，不继承旧同意或倒计时；明确不再推进该方向时停止，不反复重提。新一轮仍按首人同意启动计时。对于尚未取消、仍有效的审批期间另起草稿，建议不静默废弃旧审批；适用性与依据变化继续按 Q017 检查。

示例：A 同意、B 填写原因退回、C 没回复。按 Q023，本次提议立即取消，C 不再需要回应，也不会在截止时间被追加为超时同意。A 的历史同意与 B 的退回原因保留；其他独立问题的审批不因这张单取消而被统一取消。

超时可由普通后台计时任务按规则处理，无需主 Agent 持续运行或反复询问模型。处理时重新核对版本与实际审批状态，保留来源，避免已处理、已撤回或已变更事项重复生效。具体并发、截止边界与故障恢复在规则确认后设计。

## 20. 第七轮答复记录

**V2-Q021：CLI 回复同意怎样成为正式审批？** 是否采用“用户看到具体事项后明确同意，本地 AI 通过 CLI 代提交，直接按本人审批生效”的方式，无需再到 Web 重复确认？

用户答复：“同意”。确认采用用户明确同意后通过 CLI 代提交、直接按本人审批生效的方式，记录本人、来源、事项和版本，无需 Web 重复确认；不授权 AI 自主决定同意。具体身份与授权机制后续设计。

**V2-Q022：主管或负责人兜底同意覆盖什么范围？** 是代为处理指定的未回应审批项，还是一次就直接批准整个提案？

用户答复：“同意”。确认主管或负责人可以明确选择一个或多个未回应审批项代批，记录操作者及范围。退回处理以 Q023 为准。

**V2-Q023：超时能否覆盖明确反对或退回？** 有人通过开始计时后，另一位明确拒绝或退回，是否仍在到期时把其算作同意？

用户答复：“提议是要所有人同意才行，其中一个人点退回则作为取消，但是取消要明确提出原因”。确认所有指定审批人须满足同意，任意一人退回则本次提议取消，原因必填；不保留上一轮仅“保持待处理”的建议。没有撤销 Q020 针对未回应人的超时默认同意。

**V2-Q024：计时与同意怎样绑定提案版本和部分批准？** 是否按可独立批准的具体内容与版本计时，修订提交新审批版本后重新等待首个明确同意？

用户答复：“这种分步骤和流程的应该是作为分开同意，我说的是那种要多人审批同一个问题的情况”。确认不同步骤分别审批，超时规则适用于同一个问题的多人审批；不将此澄清当作对上一轮全部版本继承和时间默认值的确认。

## 21. 当前审批模型

为便于界面与实现统一，建议把同一个问题的一次提议称为一张审批单，多个步骤或问题各有自己的审批单；一个议题可关联多张单及其依赖。该命名不增加新的业务审批层级。

| 当前审批单发生的事件 | 结果 |
| --- | --- |
| 尚无人通过 | 等待首次同意；没有因“首人同意”启动的倒计时 |
| 第一人同意，尚有人未回应 | 开始计时，继续等待同一问题的其他审批人 |
| 所有指定审批位满足同意 | 本次提议通过；同意来源保留本人、代批或规则触发的区别 |
| 到期且审批仍有效、没有退回 | 未回应位按 Q020 视为同意，满足条件后通过 |
| 审批进行中任何一人退回并说明原因 | 本次提议取消，停止计时和其他待审批操作，保留历史与原因 |

“退回导致取消”与“缺少材料暂时等待”是不同事件；不能把讨论里提出问题自动视为退回，正式退回由有权人在审批入口明确提交。

建议 Web 和 CLI 的退回操作都要求填写原因，由服务端校验。取消与到期通过需要一致的状态检查，避免重复任务或并发操作使同一审批同时出现通过和取消。已生效的其他独立事项不自动撤销；涉及依赖变更时继续遵循正式变更流程。

## 22. 第八轮答复记录

**V2-Q025：取消后是否允许根据原因形成新提议？** 本次审批保持已取消，主 Agent 按退回原因继续整理修订草稿，并按项目流程发起新的审阅，是否接受？

用户答复：“允许。原提议保持已取消，新提议展示修改说明并重新审批，不继承旧同意或倒计时。如果原因明确是‘不再推进这个方向’，则停止，不自动反复重提。” 已确认。

**V2-Q026：同一问题多人审批的超时时长怎样设置？** 是否由项目流程配置，提供项目默认值并允许不同环节调整？

用户答复：“项目默认 24 个自然小时，环节可以调整。从首人同意开始显示明确截止时间，后续同意不重置倒计时。这个默认时长目前只是建议。” 记录项目默认/环节可调与首同意起算、不重置规则；由于答复仍保留“只是建议”，24 小时保留为推荐默认值，不宣称该数值已最终锁定。

## 23. 交付与机器人持续归属

本节同步 Q027、Q028。交付依据文字流程，执行与业务记录持续归机器人，换人不另设交接。具体界面与数据实现仍待设计，不恢复 V1 的研发专用状态机。

### 正式工作的交付约定

工作安排获批后，具体工作仍在成员本地工具或实际工作环境完成。提交材料、AI 认为已完成、成果被有权人接受，应在界面与记录中区分。

Q027 已确认由 AI 从文字流程整理交付约定，缺失时帮助补齐。建议界面呈现负责机器人、成果或目标、完成条件和验收职责，用户确认时可以补充，简单工作可采用项目已有约定，不增加反复手填字段的负担。

示例：设计工作以指定版本的素材和目标要求为依据；研究工作可以交付证据充分的结论，也可以按事先约定交付“当前证据不足及后续验证建议”；研发工作按项目约定提交实现引用与验证依据。不同领域不共用固定的“开发完成→测试通过”链条。

Q027 已确认本地成果回传后，线上 AI 对照约定提出验收建议，按已有审批规则接受后才正式完成。建议验收卡片展示满足项、缺口与对应成果版本；工作完成不由原先的安排批准自动推出。

### 机器人持续存在，人决策并可提供协作输入

Q028 已明确执行过程绑定机器人，Q029 进一步补充人可以直接作为问题、信息、证据和文件提供者参与。机器人标识、事项、业务资料、会话、岗位经验和审批过程持续存在；更换绑定人不会生成另一份任务或复制一套资料。新绑定的人直接进入同一机器人 Chat 查看历史内容和之前审批。

由这一归属规则推得：进行中的运行和业务进度不因换人中断；已批准、已取消、待决状态及原截止时间不因换人重置。未决审批继续属于对应机器人/流程职责，当前有权人可以处理；历史决定保留真实操作者，不改记到新任名下。

不再设计交接确认、迁移任务、迁移知识或接管时重开审批的流程。历史摘要可以作为任何成员随时使用的阅读辅助，不是换人的前置条件。

当前身份能否作决定仍须按绑定与授权校验，旧绑定不继续授予操作权。个人私有账号、配置与凭据不因绑定变化自动变成机器人资料；这与新绑定人直接阅读机器人业务 Chat 并不冲突，也不增加额外的业务交接门槛。

## 24. 第九轮答复记录

**V2-Q027：正式工作是否同时明确交付约定？** 确认一项安排时，是否一并明确负责机器人、要交付什么或解决什么问题、完成条件，以及由哪个职责对应的人确认接受？

用户答复：“由 AI 从文字流程中整理，缺失时帮助补齐。成果回传后，AI 对照约定提出验收建议，按已有审批规则接受后才正式完成。设计可以验收具体稿件，研究可以验收结论与依据，各领域保持自己的标准。” 已确认。

**V2-Q028：换人时是否按机器人接续业务并保留历史责任？** 新任人员是否继承权限范围内的业务资料、业务会话、岗位经验、未完成事项与待审批，前任历史决定仍保留原操作者，私人配置和凭据不自动转交？

用户答复：“应该是执行过程都是绑定在机器人上的，人在其中只是决策而已，所以换人没有任何影响，换人之后直接进入 chat 界面自然可以看到历史内容和之前审批的内容。” 记录结果：执行与业务记录归机器人，更换绑定不影响原过程；新绑定人直接看已有 Chat 和审批，删除上一轮独立交接/接续流程的设想。

## 25. 机器人 Chat 的第十轮确认

Q029 已确认界面组织与人工输入入口。2026-09-25 用户已选定灵感工作室并要求删除其他方案，当前范围见 [工作室交互闭环](./工作室交互闭环.md)；真实 Agent 运行机制仍待实现。

已确认机器人页聚合参与议题与待决事项，进入具体议题后查看对话、成果和内嵌审批卡片；“待我审批”支持直接操作，也可跳回审批记录所在的对话位置。换人后访问原有机器人与议题记录，不另建新人的会话副本。

已确认议题 Chat 下方提供输入框，人可以发送问题、信息、证据或文件辅助机器人。人工参与是已有机器人流程的输入之一，不以每项工作都必须有人工发言为前提，也不改变本地事项须手动调用 Skill 获取的规则。

同一议题在不同入口展示相应有权查看的记录；各子 Agent 的独立探索上下文仍单独保存，不能因界面聚合就把所有原始上下文广播给每个参与机器人。

**V2-Q029：机器人 Chat 如何呈现多个议题？** 机器人入口是把所有工作汇成一条长期对话，还是先列出参与议题，点击后进入各自的 Chat？

用户答复：“第二种。机器人页面聚合议题与待决事项；具体 Chat 展示该议题的讨论、成果和审批卡片。‘待我审批’可以直接操作，也能跳回对应位置。换人后进入的仍是这些原有记录。” 补充：“然后下方应该是有个输入框，也就是人工可以参与其中作为消息问题，信息，证据或者文件提供者可以辅助机器人。” 已确认。

## 26. 议题输入框与第十一轮确认

本节同步 Q029、Q030。人工输入与普通聊天/正式审批的边界已确认；2026-09-25 的前端原型已有消息、附件上传/拖放与预览，引用消息等其余交互仍为建议。真实资料处理与业务后端未接入。

### 页面组织

建议议题 Chat 顶部显示议题及当前身份，中部展示人工消息、机器人返回、成果与审批卡片，底部使用统一输入框。输入框可上传或拖放附件，也可引用已有消息/材料补充说明。引用、拖放等为实现建议，不强制先选择“问题/信息/证据”等类型。

消息保留实际发言者、时间与来源；人工发言不伪装为机器人已经得出的结论，本地工具上报保留工具来源。人与机器人提供的材料都可进入同一条可追溯的议题记录。

### 输入后的处理

- 普通问题：主 Agent 根据当前议题回答，必要时按流程通过 `call_agent` 获取相关机器人的分析。
- 补充信息或纠正：保留原消息，更新本轮讨论依据和草稿；若影响正式安排，提出相应变更提议。
- 证据或附件：记录原件、来源和当前 Chat 上下文，AI 在权限内读取并引用；无法解析时展示具体限制，不能把上传成功等同于已经理解或接受。
- 工作指示：可以引导允许范围内的分析，如“请对照这两份稿件”；正式分工、范围和成果接受仍按既有决策规则处理。

以上处理由主 Agent 居中组织，输入框不额外建立绕过项目流程的子 Agent 直连通道。附件的存储和可见性继续遵循已有资料权限，不能因为发了一条消息就扩大未获授权的访问。

依 Q015，相关新消息可触发或续接平台内的一轮协同。建议连续发送的文字和附件合并处理，避免每条消息都开启全体机器人讨论；人不必每次选择需要调用哪些机器人。运行中的资料更新应能被后续分析发现，已经生成的提案仍按 Q017 的版本规则处理。

### 普通发言与审批动作

Q030 已确认普通输入框用于交流和提供材料。“我觉得可以”“同意这个方向”等普通消息保留为意见，不由 AI 自动写成正式批准。Web 的正式决定通过对应审批卡片操作，CLI 继续按 Q021 支持本人明确指定事项并同意后直接代提交。

普通消息中的“退回”或不同意见也不直接取消提议。正式退回通过对应审批操作提交并填写原因；后端按已经确认的取消规则处理。需要用户决定时，机器人可以指向相应审批卡片，但不能把自己的意图识别结果当作审批事件。

**V2-Q030：普通输入框里写“同意/退回”，是否直接视为正式审批？** 是否让一般消息保持讨论性质，正式审批通过明确的审批卡片操作，CLI 沿用已确认的本人明确指令方式？

用户答复：“普通聊天保持讨论性质，正式决定通过对应审批卡片操作。这样‘我觉得这个方向可以’不会被误记成批准某个版本。已确认的 CLI 能力保持不变：本人明确指定事项并同意后，可以直接代提交审批。” 已确认。

截至本节的 Q001～Q030 均有答复，后续 Q031～Q033 的业务模型确认见第 29 节。应围绕 [产品主线](./产品主线.md) 核对场景与实现，不因某一轮问答完成就宣称所有工程问题已解决；24 小时仍为推荐默认值，具体技术依赖与可运行验证尚未完成。

## 27. 灵感工作室的选定与深化

2026-09-25 用户明确：“我认同灵感工作室这套交互方案，其他的你都删了然后继续深化下这套交互的逻辑和闭环”。

已按该选择删除当前另外两套 ToC 页面、对比入口和专用样式，根入口直接进入工作室。7 张旧预览图片的本地删除被工具审批策略拦截，仍在未被页面引用的忽略目录中。历史 V1 设计仍只作为原先明确归档的材料，不纳入当前实现。

本轮前端将“方向通过”和“成果验收完成”分开，增加工作约定、本地简报、交付版本、验收提议、项目汇总，以及项目资料登记后人工确认归属。阶段与下一步由实际演示状态推导；新资料使旧待审依据失效时，需要核对修订后再批准。

普通消息继续不触发正式决定；退回取消、修订重提、明确范围的负责人代批、换人不重置历史与计时均沿用已确认边界。本轮的细化与完整模拟范围见 [工作室交互闭环](./工作室交互闭环.md)，不宣称真实模型、CLI 或生产授权已接入。

## 28. 项目机器人、邀请与不同菜单视角

2026-09-25 用户指出上一版将机器人创建、人员绑定和个人设置混在同一管理视角，应改为项目创建机器人，参与者加入后以机器人身份工作，并设置本人的项目工作提示词。创建人或管理者与加入者需要不同菜单。

针对加入方式，用户进一步明确：“加入都是管理员邀请加入，然后管理员邀请的时候可以选多个机器人，人受邀请之后直接加入然后可以配置自己的项目工作提示词”。此答复替代先前“成员加入时自选机器人”的设想。

**已确认的产品方向**：项目拥有机器人；邀请由管理员发起并指定多个机器人；受邀人直接加入承接，之后配置自己的项目提示词；管理者与成员使用不同操作视角。岗位职责、业务记录和个人偏好仍分开归属。

**本轮 Demo 细节**：创建机器人时不填人；管理员邀请时勾选尚未分配的机器人，邀请期间预留；受邀人点击接受后一次承接全部指定岗位，无第二轮入项审批。支持撤销邀请、已加入成员间调整承接人，以及创建人授予或撤销管理角色。待接受邀请时不开放业务内容，成员不能修改岗位提示词或访问成员管理。以上实现用于评审交互，真实身份、邀请分发与并发事务还未接入。

「轻笺」项目新增顶部身份切换：创建人、管理员、测试成员、待接受邀请成员。邀请成员预先指定前端与文档两个机器人，便于直接观察接受前后菜单与个人设置的变化。机器人配置、邀请与审批状态测试见 [Demo 工程](./demo/README.md)。

## 29. 计划、任务与议题的业务分工

用户指出当前只有议题，计划与任务消失，并明确要求先讨论、再讨论如何修改界面。只读核对发现，当前 Demo 只有 Project → Topic，Topic.work 内嵌负责机器人、交付约定与验收记录，没有独立 Plan / Task。Approval.kind 中的 plan 只是工作安排审批类别；项目进度按完成议题数计算。此前 Q029 仅确认议题 Chat 的组织方式，未确认用议题取代计划或任务。

**V2-Q031：计划是否有自己的目标与完成标准？**

用户答复：“计划代表阶段性交付，有自身目标、范围和完成条件。任务是其拆分的详细需要完成的内容”。已确认。其后的 Q035 已进一步确认计划整体验收；不把后续确认倒推为 Q031 当时已回答所有细节。

**V2-Q032：任务按岗位动作拆，还是按交付成果拆？**

用户答复：“按可独立交付的成果拆任务。‘任务创建功能’可以经历开发、测试、验收，由不同机器人参与；‘搭建测试环境’‘产出独立测试方案’有自己的交付物，可以另建任务。这样既能表达多岗位协作，也不会每经过一个人就产生一条新任务。” 已确认。

**V2-Q033：是否允许议题只得到一个结论，不产生任何任务？**

用户答复：“允许。议题负责讨论与结论，计划、任务记录正式工作安排。议题可以只形成结论；需要新增或修改工作时，继续走已确认的提案审批规则。议题结束本身不计入交付进度。” 已确认。

本轮已明确业务分工，不意味着原 V1 的版本号顺序、Git 规则、任务状态机自动恢复。成果回传、机器人稳定身份、草稿经有权人确认等既有 V2 边界继续有效。仅同步文档，没有修改模型代码、菜单或页面。

## 30. 计划任务关系的本轮答复（Q034～Q037）

本节保留上轮问题与建议，并逐项记录用户实际答复。Q034～Q036 已有确认，Q037 改变了职位和流程模型，原固定单个主责的建议不能视为已获接受；状态推进、依赖阻塞、范围变更、进度计算和具体菜单继续随新模型讨论。

| 已确定的前提 | 本轮结果 | 后续依赖问题 |
| --- | --- | --- |
| 计划承载阶段性交付，任务承载具体成果 | Q034：常规任务归计划，临时任务可直接归项目 | 唯一归属、任务移动、跨计划复用和进度归集 |
| 计划有自身完成条件，任务成果按约定验收 | Q035：保留计划整体验收 | 计划状态、最终验收职责、验收后的变更 |
| 议题可以只形成结论，不计交付进度 | Q036：同项目内双向多关联 | 变更影响范围、上下文入口、决定回写与版本核对 |
| 同一任务可由不同机器人参与，执行归机器人 | Q037：改为用户定义职位模板、多人任职与流程挂载 | 工作身份持续性、同岗分派、节点职责、主责与审批 |

**V2-Q034：每个正式任务是否必须归属计划？**

例如一次临时资料核查或紧急问题处理，尚未确定放入哪个阶段性交付，能否先作为项目下的独立任务存在？

上轮建议：常规任务归计划；允许临时任务先直接归项目，之后经确认纳入计划。每个任务最多有一个归属计划，其他计划可引用其成果或建立依赖，不复制同一任务重复计数。独立任务仍遵守正式安排的确认和成果验收规则；原始报告可以先登记。

用户答复：“常规任务归计划，允许临时任务直接归项目”。仅此归属例外已确认；“最多一个所属计划”、后续移入计划及跨计划复用的具体方式仍是建议，不能一并记为用户接受。

**V2-Q035：任务都验收后，计划是否还需要一次整体验收？**

计划有自己的目标与完成条件，但这不自动意味着必须再加一次人工验收，也不等于所有任务完成后计划自动完成。

用户答复：“保留一次计划整体验收。AI 汇总已验收任务及其证据，对照计划目标和完成条件提出验收建议，由流程指定的有权人确认。这里检查整体结果，复用任务证据，不重做各项任务验收”。已确认。

**V2-Q036：同一个议题能否关联多个任务，甚至多个计划？**

例如一场延期讨论同时影响开发、测试和发布安排；同一任务也可能先后有需求澄清、故障分析和验收争议等不同议题。

用户答复：“在同一项目内允许双向关联。一个议题可以关联多个计划或任务，一个任务也可以关联多个议题。各入口查看同一份讨论和决定，避免复制聊天、重复审批；正式工作变更仍经对应提案确认。” 已确认。关联权限与 AI 新增关联需人工确认的既有边界继续有效。

**V2-Q037：每个计划和任务是否需要一个持续负责的主责机器人？**

多岗位参与时，区分谁持续跟进整体交付，以及谁负责当前环节；例如功能进入测试后，是否把任务整体负责机器人也改为测试机器人？

上轮建议为每个计划、任务设一个持续主责机器人。用户没有确认该建议，而是修订了前置模型：

> 没有所谓的测试机器人，各个机器人是用户手动设定的可以是任意机器人，用户创建项目中可以设定对应的什么任务应该交给 xxx 机器人。

用户进一步说明：项目可以建立多个机器人模板；邀请多人作为同一个机器人时，项目内存在同模板的多份机器人，关联人不同。界面看起来应是各个关联人的列表，可以把机器人理解为职位，关联人承担职位；该职位对应的 AI 结合机器人提示词和个人提示词工作。

对于项目工作流程，用户要求通过与 AI 沟通生成多张 Mermaid 流程图，工作或计划的任务沿流程流转；创建机器人时可下拉选择挂载到哪条流程，让代码参与硬性规范的校验。以上方向已记录，具体模板/身份关系及流程语义尚待下一轮澄清。

## 31. 职位与流程模型的答复（Q038～Q041）

本节记录 Q038～Q041 的实际答复，并按第 32 节最新补充解释流程用途：流程指导 AI 讨论与提出推进建议，正式工作状态由用户上报或操作。不能将模板、成员账号、持久工作主体混为一体，也不能把同版规则解释为无人确认的自动业务引擎。

**V2-Q038：新增同岗成员与替换既有工作身份的关联人，是否分开？**

例如“内容策划”模板已有林然和周宁。新增第三人应增加一份身份；如果林然离开，接替者是接续林然原来的业务身份，还是仅因选了同模板就从空白身份开始？这需要与 Q028 已确认的换人后工作和会话持续存在保持一致。

本轮建议为：模板定义共享岗位职责，具体任职身份持续持有自己的工作和业务记录，当前关联人可替换。新增成员产生新任职身份；替换则更新原身份的关联人，保留历史与待办，不增加交接审批。界面可显示“林然 · 内容策划”，个人提示词仍归本人；同模板不自动共享各人的全部会话与私人配置。

用户答复：“认同”。已确认。

**V2-Q039：职位挂载流程，是否还需明确可参与的具体环节及职责？**

只选择“交付流程”，仍无法据此判断这个职位在哪一步能提交材料、提供意见或作出审批。

用户答复：“我的意思是明确挂载到那个流程的那个节点”。已明确绑定粒度为流程与节点。结合后续补充，该绑定首先用于让 AI 知道当前步骤可以选择哪些职位与任职身份参与讨论，不擅自扩展为上轮建议中的全部节点动作权限或自动迁移规则已确认。

**V2-Q040：是否接受由同一份可校验规则生成 Mermaid 图，而不将任意图中文字直接作为执行依据？**

Mermaid 提供图定义、语法解析和渲染；谁可执行哪个动作、完成条件和审批等业务约束需平台补充。它也可作为受限工作流语言的输入，但必须明确定义额外语义，不能把图语法通过当成业务规则验证通过。官方依据见 [Agent 协同架构讨论](./Agent协同架构讨论.md)。

用户答复：“用户通过对话制定流程，AI 生成明确的流程规则，并据此生成 Mermaid 图和环节说明。人确认后，图与执行规则以同一版本生效。用户不用手写配置，代码也不必运行时猜测箭头旁文字的含义”。已确认图与规则的来源和版本关系。后续又明确图供 AI 参考，不能把“执行规则”扩大解释为模型或引擎无需人的决定即可按边推进。现有工作如何采用新流程版本仍待细化。

**V2-Q041：多张图是否分别定义不同种类的工作流程，同一项工作共同沿一份流程推进？**

例如用户定义“计划交付流程”“方案制作流程”“问题处理流程”。同一条任务由多人参与时，是各人运行一份独立进度，还是围绕该任务共用一份业务流程进度？

用户答复：“计划和不同类型的任务选择适用流程，每个具体计划或任务有自己的流程运行记录。多人共同推进这份工作；各人的 AI 可以保留独立探索会话。同一职位有多人，不因此复制任务或产生多份互不一致的交付进度”。已确认。运行记录保存实际业务过程，不因这个名称就意味着引擎自行执行全部转移。

## 32. 流程参考、本人更新与推进卡片的补充

用户在 Q038～Q041 后继续明确了运行方式，本节优先于此前可能被理解为固定流程引擎的建议：

1. 项目流程图类似知识库与系统提示词，是 AI 根据实际情况抉择的参考。每次项目模型调用均应提供项目流程及节点绑定信息，不只在项目建立时读取一次；具体注入形式和上下文优化尚未实现。已确认的资料权限与私人设置边界仍有效。
2. 职位明确挂到某流程某节点，AI 据当前步骤从相关职位/任职身份中选择讨论对象。组织分析讨论与改变已经批准的工作分配是不同动作；正式分工变化仍沿用 Q007、Q033、Q036 的确认边界。
3. 计划与任务创建出来时已经分配关联机器人。任务状态由相应成员通过本地 AI/CLI 主动推送或手动更新；计划创建时设负责人，计划状态由负责人通过同样渠道更新。平台 AI 的判断本身不直接取代这些正式操作。
4. 计划可呈现树形任务列表并具有前置关系。前项完成后，由 AI 根据流程和实际内容判断下一步，形成推进建议；完成前项的人点击同意，才推进给下一人。这里的自动性指触发分析和建议，发送人的确认不能省略。
5. 推进卡片双方可见对应发送人、接收人和“基于哪条流程”的依据。流程版本、节点、成果与理由等卡片字段是后续可细化内容，不能把“接收方看到卡片”直接记为接收方必须再审批。

后续 Q042～Q049 已确认完成层次、两种推进、树与依赖分离、计划负责人整体验收、接收人明确接受后完成交接、未最终验收交付退回待完善及多来源逐项拒收。当前仍待细化的是未回应、部分接收保留、已最终验收结果重开、已发卡片遇到新版流程等细节；不重复询问已确定的原则。

本轮仅同步讨论文档，不修改界面或业务代码，不启动实际工作流或成员本地 AI。

## 33. 完成、推进与接收的答复（Q042～Q045）

本节保留上轮问题并记录实际答复。Q042、Q043 的建议已获认同；Q044 明确拒收权，未一并接受此前全部接收方案；Q045 明确负责人就是整体验收人，替代此前允许两者分开的建议。AI 选择讨论对象仍不等于擅自修改正式分工。

**V2-Q042：成员上报“完成”，指当前工作环节完成，还是整项任务最终验收完成？**

同一个任务可能由多个任职身份参与。一个人完成当前部分之后还要交给下一人时，其“完成”与最终任务完成应如何区分？这也需要衔接 Q027 已有的成果接受规则。

本轮建议：按当前职责上报环节交付完成；需要验收的任务，由指定有权人确认后才正式完成。若同一人同时有交付与验收职责，可通过一次明确操作完成两项业务事件，不强迫重复点击。成员明确上报、AI 判断与有权人验收分别留痕。

用户答复：“认同”。已确认。

**V2-Q043：推进是否同时支持同任务换环节与启用下一个独立任务？**

“方案制作交给审核”可能是同一任务的不同环节，也可能是前项独立任务完成后启用另一项有独立交付物的任务；两者不应仅因卡片都显示“交给下一人”就混为一个状态。

本轮建议：支持两种明确关系，卡片标清是同任务进入下一环节，还是前置完成后推进另一独立任务。任务树表示工作拆分，前置关系表示先后依赖；同级任务可并行，不按树上显示顺序直接串行推进。

用户答复：“认同”。已确认上述区分，具体多个前置如何汇合仍待细化。

**V2-Q044：已分配的后续工作，发送人同意推进后，接收人是否还必须确认接收？**

用户已经确定双方能看到来源与流程依据。上一轮询问接收卡是已分配工作的待处理入口，还是明确接受后才生效的请求。

上轮建议是已确认分工内不额外增加一轮批准，并提供退回或补充请求。本次用户答复：“可以拒绝，因为任务完成不一定是真的完成，接收人拒收可以说明原因让发送人继续完善任务”。

此时确认的是针对交付可用性的拒收权，反馈回到发送人以便完善；其后的 Q046 已进一步确认必须明确接受，Q047 确认未最终验收交付的退回范围。超时接收与已正式验收工作重开仍未确认。

**V2-Q045：计划负责人是否必须同时是计划验收人，还是由流程分别指定？**

已确认由负责人更新计划状态，也已确认计划整体结果须由有权人验收。现在需要明确的是两个职责的关系，而非再次决定要不要整体验收。

用户答复：“是的负责人就是整体验收人”。已确认计划负责人承担计划整体验收与状态更新，两者不作为常规分开的岗位。此前“允许分开配置”的建议被此答复替代；不再设计另一位常设计划验收人先通过、负责人再单独改完成的默认路径。该答复不把所有项目管理员自动变成任意计划负责人。

本节答复后的正常接收、未验收交付退回、多来源确认与流程新版选择，已在第 34 节继续确认。接收提醒与异常细节仍需后续讨论，界面与代码未改。

## 34. 接收与异常边界的答复（Q046～Q049）

本节记录 Q046～Q049 当轮答复及其范围；其后的接收提醒、任务重开、部分来源接收保留和旧卡片改版处理，已在第 35 节由 Q050～Q053 继续确认。两轮结论分别保留来源，不把后续确认倒推为此前已全部确定。

**V2-Q046：正常情况下，接收人是否需要明确接收，交接才算完成？**

拒收已经明确；如果接收人尚未查看或处理卡片，是已经接手、只是还没开始，还是仍处于待接收？

用户答复：“发送后进入待接收，接收人明确接受后才算交接完成”。已确认。上轮提到“开始处理”可同时记为接收的快捷方式未被明确接受，后续不得据此把任意处理动作或普通聊天自动视为接收。

**V2-Q047：拒收退回的是本次交付，还是可以直接重开已经验收完成的任务？**

尚未最终验收的环节交付与已经正式验收的独立任务，需要明确各自被拒收后的影响，避免一条退回抹掉整个计划的既有结果。

用户答复：“尚未最终验收的交付，直接退回待完善，相关后续推进暂缓，无关任务继续”。已确认这一范围。上轮关于已最终验收任务如何复核和重开的建议没有在此次答复中得到确认；不能把本条扩大为全部已完成工作可被自动回退。

**V2-Q048：多个前置共同到位时，由谁确认交给下一人的整份材料？**

例如文案与图片由不同人负责，二者完成后才能进入排版。应由各发送人确认自己的交付，还是由一个已授权的统筹人确认整包发送？不能默认最后完成的人有权代表其他人。

用户答复：“各发送人确认自己的交付，AI 汇总后呈现给接收人，接收人可以选择拒收某一个人的内容”。已确认逐来源确认与选择性拒收；不能只保留整包接收/整包退回。统筹人替所有来源默认确认不在此次答复范围。来源之间存在依赖时的影响评估、部分接收如何保留、补交核对范围继续细化。

**V2-Q049：流程发布新版本后，正在进行的计划、任务和已发出的推进卡如何使用它？**

新流程可能改变下一步的候选职位、推荐路线或提交条件。用户要求每次模型调用参考流程，但人的决定又必须绑定实际审阅版本，不能以新版默默替换旧卡的含义。

用户答复：“按照新版推进也就是提示词直接修改”。已确认新版流程直接更新后续调用的提示词，不将逐计划/逐任务确认“切换流程版本”作为前置要求。此前建议的逐事项采用确认不再作为默认设计。

Q040 中规则与图经人确认同版生效，以及 Q017 中人的决定绑定实际审阅内容的原则继续保留。新版参考直接采用，不等于可以改写已经发生的人的决定；已发卡片受新版影响时的处理已由后续 Q052 确定，见第 35 节。

本节后的部分拒收接收保留、未回应处理、旧卡片受新版影响及最终验收后重开，已在下一节继续确认。界面仍待业务关系讨论完成后调整。

## 35. 逐来源接收与变更的答复（Q050～Q053）

用户对 Q050～Q053 均答复“认同”。以下记录获接受的规则；没有因此开始界面或代码修改，具体技术实现和业务权限仍按前述边界执行。

**V2-Q050：只拒收一个来源后，其它已经接收的来源是否保留原结果？**

例如文案已接收、图片被拒收，图片修正后补交，是否需要文案一方重新发送、接收人重新接受全部材料？

已确认：未变化且未受补交影响的已接收来源保留原结果，只重新核对补交部分。若新内容确实影响其它来源的适用性，AI 明确指出影响，由相关人复核；不因为一份补交就将全部接收意见归零。保留某一来源的接收，不等于整体必需前置已满足。

用户答复：“认同”。

**V2-Q051：接收人长期未回应时，是否提醒并交由计划负责人协调？**

明确接受才完成交接已经确定，但持续待接收需要有人跟进。原多人共同审批的超时同意不能自动解释为此处的接收规则。

已确认：项目可配置提醒时限，到期提醒接收人和计划负责人，保持待接收；负责人协调处理，必要时按权限安排改派，不自动记为原接收人同意。已有“不主动触发成员本地 AI”的边界继续有效；本轮未锁定具体提醒时长。

用户答复：“认同”。

**V2-Q052：新版改变已发卡片的接收人或交付要求时，是否重新确认改变的交接内容？**

后续采用新版提示词已确定。这里要处理的是一张基于旧内容已经由发送人确认、但尚未接收的卡片，不能把卡片内容被改变后的效果当成发送人原先同意的范围。

已确认：没有实质影响的卡片继续处理；受新版实质影响的卡片由 AI 更新建议，发送人确认改变后的内容，再交给接收人，原记录保留。新的确认针对改变后的交接内容，流程提示词本身直接更新，不恢复逐计划/任务批准切版的旧建议。

用户答复：“认同”。

**V2-Q053：已经最终验收的任务发现问题后，是否允许由有权人确认重新打开？**

Q047 只确定了未最终验收交付的拒收退回。正式验收后发现遗漏或缺陷时，仍需要明确是否重开原任务，不能把之前的未验收退回规则直接套用。

已确认：允许当前有权验收的人确认重新打开任务，保留上一轮验收记录，修正与复核作为新一轮记录。超出原约定范围的新需求另建任务，不将新增范围混成原交付不合格。对已完成计划及其它工作的影响，应继续遵守计划负责人更新状态与无关工作不自动回退的既有规则。

用户答复：“认同”。

本轮已明确局部补交、接收未回应、新版影响旧卡和最终验收后重开的处理原则。剩余问题应只围绕真正影响模型的未决关系，不重新询问这些规则；展示和技术细节不自动扩展为新的审批。当前只更新文档，不修改代码、界面或运行中的 Demo 数据。

## 36. 依据已确认规则整理的含义与最后两项答复

### 无需重复询问的含义

- Q042 区分职责交付与最终验收，Q046 将明确接收定义为交接完成。因此普通接收不会隐式完成来源任务的最终验收；若后续界面明确组合多个动作，需同时满足各动作的权限与条件，并分别留下记录。
- 计划状态由负责人更新，负责人就是整体验收人。因此任务重开后，可以呈现当前任务事实及计划需复核的提醒，不能由 AI 自行把计划正式状态改回进行中。负责人依据整体影响更新计划，历史验收不被擦除。
- 身份、资料范围、审批资格、明确的发送/接收与验收决定、实际审阅版本，都应按既有确认进行代码核验。Q055 又明确用户确认为硬性要求的业务前置也需检查，普通推荐路线仍作为模型参考，不将所有图形连线固化为强制顺序。

以上是根据既有答复整理的实施含义，不新增审批。用于本次计划/任务界面设计的最后两项模型选择如下，用户均已答复“认同”。

**V2-Q054：一个任务是否最多归属一个计划，跨计划使用通过引用或依赖表达？**

Q034 确认常规任务归计划、临时任务可直接归项目，尚未确认一个任务是否能同时出现在多个计划中作为正式所属任务。这影响计划树、验收归集和进度计算。

已确认：一个任务最多归属一个计划，临时任务可以直接归项目；其它计划引用其成果或建立依赖，共用原任务和验收记录，不复制同一份工作。引用不会自动成为引用方计划的验收通过，该计划负责人仍检查本计划的整体条件。

用户答复：“认同”。

**V2-Q055：明确确认的必需前置或必交资料，是否由代码阻止条件未满足的推进？**

流程图作为 AI 参考已经确定，不能把每条 Mermaid 连线都当硬顺序。但如果用户已经明确确认“必须收到某份交付并接收后才能推进”，AI 认为可以提前时，是否仍需先正式调整该要求？

已确认：用户明确确认为硬性要求、且能被明确校验的业务条件由代码检查，例如指定交付已有明确接收记录、必需材料已提交。普通推荐路线继续由 AI 结合实际建议；要放宽已确认硬条件，先由有权人明确调整，再推进。代码不凭材料存在就保证成果质量，内容质量仍由 AI 分析与人判断；不能因某个词或某条连线自动创建用户未确认的硬性要求。

用户答复：“认同”。

其它尚未实现的事项，如按钮文案、提醒频率、流程字段编码、工具协议和事务一致性，属于后续界面/工程设计，不以它们为理由重复追问已确认的业务规则。本轮仍不修改 Demo。

## 37. 本轮业务模型收束与界面方案

V2-Q031～Q055 已将计划、任务、议题、任职身份、流程参考、硬性要求、交付、交接、验收及异常路径区分清楚，可据此形成可审阅的界面结构。此处收束的是本次计划/任务界面的业务关系，不宣称所有产品场景或技术实现已经验证。

界面建议见 [工作室界面重构方案](./工作室界面重构方案.md)。该文档把已确认业务规则与页面组织建议分开：计划/任务成为工作入口，议题承载关联讨论，成员按人与职位展示，交接卡逐来源处理。仍保留灵感工作室视觉方向；本轮只形成方案，未修改前端、服务或浏览器演示数据。

## 38. 用户授权完善 Demo 与新版 ToC 对比

用户随后提出：“demo 界面完善一下，或者看下是否需要重新设计，如果需要重新设计再出几个不同版本的 TOC 的界面给我看下”。

据此，本轮评估了旧 Topic.work 结构与新模型的差距，并制作三个共享业务数据的入口：灵感工作室 2.0、协作信箱、计划地图。它们分别从项目计划、待处理交接、任务关系进入，不复用此前删除的 Orbit/Focus 页面。用户尚未重新选定唯一主入口。

计划、任务、议题、职位模板、稳定任职身份和逐来源交接已作为独立前端模型实现；新增真实界面操作与状态校验，仍用虚构资料和模拟 AI。旧版在 legacy 保留，新数据键不覆盖原演示数据。具体体验、验证结果与尚未实现的能力见 [新版 Demo 方案对比](./新版Demo方案对比.md)。

## 39. 回到 Chatbox 交互草图

用户明确反馈上一轮三套交互差异不够。新的界面方向是：先看到项目列表，进入后以聊天为主；多个讨论节点可以像浏览器标签一样打开，甚至同时查看；右侧作为多功能展示区。任务树希望以真正相连的卡片呈现，依赖关系尽量在同一视图说明。

用户要求暂不做全套功能，先做几个简单 HTML 讨论清楚操作方式。本轮据此新增“标签会话台 / 并排协作台 / 主会场与侧聊”三份隔离草图，保留原应用。它们是待比较的交互建议，不是新增或改写的业务规则。具体入口和边界见 [Chatbox 交互草图](./Chatbox交互草图.md)。

## 40. 选定标签会话台并调整空间

用户明确选择“标签会话台”，希望左、中、右空间由用户自由拖拽分配。据此继续深化该主模式：项目与讨论在左侧，会话标签、消息和决策卡在中间，卡片树与依据在右侧。新增分隔线拖动、宽度记忆、独立收起恢复和快捷布局。其他两种聊天模式保留为历史比较，不再默认并列推进。

本轮只改变界面组织与布局，不改变任务更新、交接接收、最终验收或权限规则。成员与职位配置等完整页面如何嵌入侧栏仍属于后续界面设计，尚未宣称已实现。

## 41. UI 重整与功能进入会话工作区

用户进一步要求重新调整整体 UI，并检查所有需要的内容如何放入已选定的交互。当前以 React 会话工作区承接静态草图，复用原有工作数据、权限校验和业务动作；原静态草图保留参考。

中间负责讨论与正式交接卡，右侧承载计划任务、资料、团队职位、流程、个人提示词、待处理和记录；左侧保留项目与会话导航。管理员可为已有成员明确分配职位，让新建项目创建人也能承担自定义岗位，不改变原有管理与验收权限分工。

AI 运行、真实 CLI、记忆权限和多人会签的完整整合仍未完成，不能把入口视为已落地能力。功能映射、实际可操作范围与缺口见 [会话工作区整合设计](./会话工作区整合设计.md)。


## 42. 回归灵感工作室 2.0

用户明确要求回到灵感工作室 2.0，清理其他交互方案；计划任务树改为从左向右的卡片树，任务列表改为卡片。当前按这一选择实施，不再并列推进 Chatbox 或会话工作区。树状呈现不限于两个子节点，归属和前置依赖分别表达；新增子任务继续作为草稿确认。实现、清理范围、闭环检查与后续改进见 [工作室 2.0 卡片树与闭环](./工作室2.0卡片树与闭环.md)。


## 43. 技术方案第一轮确认：Q056～Q060

用户要求先讨论前端组件、Go / Rust、数据库、单体、OpenSandbox、pi 参考与 K8s 部署，待讨论后再写入详细设计。

- Q056：UI 希望统一一个库的来源，Demo 只是交互验证，不强制工作室视觉。
- Q057：后端采用 Go，HTTP 使用 Gin。
- Q058：参考 pi 的四工具、上下文压缩与拼接等机制；其他业务 Agent 工具单独实现，不直接决定嵌入 pi 的 TypeScript 包。
- Q059：SQLite 留给本地缓存或执行临时用途，文件使用对象存储；结合 Q060 的 PostgreSQL 选择，平台持久数据按 PostgreSQL 收束。
- Q060：公司内部私有使用，K8s 部署整套内容，建议 Helm；区分开发与生产配置，生产可选择内置或外部 PostgreSQL 和对象存储。

## 44. 第二轮确认与注册制度：Q061～Q064

- Q061：明确确认 HeroUI 作为统一基础 UI 库。
- Q062：明确确认内置对象存储使用 SeaweedFS。
- Q063：首期即注册制；公司人员自由注册、自由创建项目；所有账号在平台业务层平级，只在对应项目中确定角色与职责。
- Q064：接受 Zustand 管理本地交互状态、TanStack Query 管理服务端数据的分工。

本轮 Q063 原提问涉及可用性，但用户答复确定的是注册与角色制度，没有确认生产单实例、允许停机或高可用目标。详细设计将这些可用性问题继续列为待定，不把推荐方案误记为已同意。

注册与项目加入区分：此前项目管理者邀请、指定职位的规则未被取消，注册不自动获得其他项目成员或审批权限。平级账号也不意味着项目内部没有管理与验收职责。

## 45. 授权写入详细设计

用户明确要求先将这些内容落入 `docs/详细设计`。据此创建设计基线，按产品身份、业务流程、前端、后端、数据、Agent、API、Helm、测试和待定事项分章。

已确认选择与工程方案分别标记。当前 Go Runner 分工、API URI、数据库表名、React Flow / ELK、pgx / sqlc、River 等属于设计方案或候选，不能当成用户已逐项确认。没有同时安装组件、修改 Demo、实现后端或部署 K8s。


## 46. Agent 运行决定：Q065～Q069

- Q065：认可已有任务优先使用正式分配身份；探索讨论可按职责选择一个或多个获准身份，不自动变成正式工作安排。
- Q066：认可部署统一提供可用模型与凭据，项目在允许范围内为职位选模型，默认继承项目设置。
- Q067：认可结果逐个返回、主 Agent 先处理已有结果和独立分析；必要参与者未返回时不能宣称核对完成，同一主会话串行续接。
- Q068：认可首期保持中心协调，不开放岗位 Agent 自由互聊；先验证完整闭环，受控讨论小组作为后续增强候选。
- Q069：首期暂时没有必须接入的外部 Agent 服务。当前设计不强制内部 A2A，保留未来执行适配边界。

用户要求检查上述内容是否已进入详细设计，并继续讨论工作目录和提示词如何动态拼接。检查发现第 06 章已有上下文和目录原则，但尚未完整记录这五项决定；本轮已同步第 06、10 章和总览。

新增第 11 章为“设计建议”：平台事实、授权执行目录和当前上下文分层，必带 / 必查 / 按需信息区分，材料目录与版本清单、委派返回包、异步结果和压缩恢复。文件名、具体工具接口及上下文预算未作为新的用户确认；读取日志不代表模型理解，不能承诺模型绝不遗漏。


## 47. SQLite 执行上下文与项目共享资料：Q070～Q075

- Q070：认可 SQLite 承载结构化上下文和执行副本，文件系统主要承载真实产物与临时文件；平台事实仍以 PostgreSQL 为准。
- Q071：用户提出项目 / 计划共享对象桶只读挂载，文件统一可读，修改只派生到工作目录；这一问题未直接采用上一轮逐文件准备与授权建议，最终范围由后续确认收束。
- Q072：允许分析需要时使用沙箱浏览器检查 HTML 视觉与交互。
- 额外明确：OpenSandbox 使用容器沙箱，与当前服务在同一 K8s 集群部署。不推导为业务和沙箱同 Pod，也不推导开发生产必须共用集群。
- Q073：共享资料区以项目为单位，计划相关文件按文件名称组织，不建立计划级共享权限边界。
- Q074：项目内取消逐文件授权，保留项目之间隔离；私人提示词、密钥和私有执行上下文不进入共享区。
- Q075：认可明确输出 / 主动提交后登记新的共享草稿，临时工作区不实时公开，原件不覆盖，正式采纳仍按既有规则。

本轮同步详细设计第 01、05、06、07、08、09、10、11 章与总览，替换大量上下文小文件和逐文件读取授权的旧方案。项目 Bucket / 外部 Bucket 前缀、挂载驱动与缓存参数仍属于实现方案；共享可读不代表模型已经阅读、理解，也不授予业务审批权限。
